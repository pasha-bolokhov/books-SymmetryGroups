        ГЛАВА 9

        ИНДУЦИРОВАННЫЕ ПРЕДСТАВЛЕНИЯ И РЕЛЯТИВИСТСКАЯ СИММЕТРИЯ

        Унитарные представления, играющие особую роль в квантовой теории,
        строились в гл. 7 для полупростых компактных групп Ли. Для
        некомпактных и неполупростых групп построение неприводимых
        унитарных представлений основано на методе индуцированных
        представлений.

        {Алгебраическая конструкция индуцированных представлений.
        Унитарные представления. Простейшие свойства}

        Для любой группы

        всегда можно построить регулярные (правое и левое) представления.
        Лишь для полупростых групп удается удовлетворительно провести
        разложение регулярного представления на неприводимые. Обобщим
        схему построения регулярного представления. Пусть для подгруппы

        группы

        известно представление

        В пространстве

        функций на

        со значениями в

        выделим подпространство

        таких функций, что

        Пусть

        факторпространство классов

        Условие

        означает, что значения

        во всех точках

        можно получить из

        с помощью операторов представления

        (рис.
        ).

        Рассмотрим действие левого регулярного представления

        группы на

        и покажем, что свойство

        инвариантно относительно действия операторов

        Подпредставление левого регулярного представления группы

        действующее на пространстве функций

        называется {представлением группы}

        {индуцированным представлением}

        {подгруппы}

        Обозначим его через

        В частном случае

        пространство

        вовпадает с пространством скалярных функциц на группе

        и индуцированное представление

        эквивалентно регулярному

        Аналогично для произвольной подгруппы

        и одномерного тривиального представления

        индуцирование приводит к квазирегулярному представлению

        Индуцированное представление можно реализовать на пространстве

        функций на

        со значениями в

        Для этого всякой

        сопоставим функцию

        по правилу

        {Упражнение}.
        Покажите, что пространства

        изоморфны.

        Пусть отображение

        осуществляет указанный изоморфизм. Тогда представление

        в пространстве

        эквивалентно

        Здесь использованы свойства факторов

        Итак, операторы индуцированного представления

        в пространстве функций на

        со значениями в

        имеют вид

        (Там, где это не приведет к недоразумениям, не будем явно
        указывать символ

        в операторе
        .)

        Нетрудно убедиться, что представления, получаемые для разных
        систем представителей

        в формуле

        эквивалентны. Аналогичная конструкция возникает при использовании
        правых сдвигов и правого регулярного представления

        Далее мы убедимся, что при определенном выборе подгруппы

        и прдеставления

        индуцирование позволяет получить унитарные неприводимые
        представления группы

        Сформулируем условия, при которых в пространствах

        можно ввести скалярные произведения так, чтобы представления

        стали унитарными. Излагаемый ниже метод называется {методом Макки}

        а соответствующие представления --- {индуцированными в смысле
        Макки}.

        Начнем рассмотрение с простейшего случая, когда группа

        унимодулярна, а подгруппа

        компактна. Пусть представление

        унитарно,

        пространство функций, интегрируемых с квадратом по мере Хаара на

        Тогда скалярное произведение на пространстве

        естественно определить по формуле

        Унитарность представления

        на гильбертовом пространстве

        очевидна.

        Подынтегральное выражение в

        постоянно на классах

        поэтому факторизация меры

        приводит скалярное произведение

        к виду

        Если

        некомпактна, скалярное произведение

        теряет смысл, однако форма

        как определение скалярного произведения

        в пространстве

        остается справедливой. (Тогда унитарность

        обеспечена унитарностью
        .)

        В случае неунимодулярных групп
        и
        по унитарному представлению

        необходимо построить неунитарное

        где
        и
        модули соответствующих групп (см.
        ).
        Затем на пространстве

        стандартно строится индуцированное представление

        {Упражнение}.
        Покажите, что

        унитарно относительно скалярного произведения

        с квазиинвариантной мерой

        в качестве меры интегрирования.

        {Транзитивность индуцирования.}
        Если группа

        содержит вложенные друг в друга подгруппы

        то представление группы

        можно индуцировать как непосредственно с подгруппы

        так и через промежуточное представление

        подгруппы

        в свою очередь индуцированное с

        Оба пути приводят к эквивалентным результатам:

        {Теорема о подгруппах.}
        Рассмотрим ограничение

        индуцированного представления

        на произвольную подгруппу

        Для описания его структуры воспользуемся свойствами двойных
        смежных классов

        Напомним их.

        Если определить
        и
        то каждый двойной класс

        представляет собой дизъюнктное объединение классов

        вида

        где

        Согласованный выбор представителей классов

        означает

        Пусть

        унитарное представление,

        представление группы

        определяемое формулой

        Построим ограничение

        и индуцированное им представление

        С его помощью ограничение

        можно разложить в прямой интеграл представлений (доказательство
        см. в
        ):

        Для анализа приводимости индуцированных представлений нам
        потребуются свойства сплетающих опереторов для представлений,
        индуцированных с разных подгрупп.

        {Теорема о переплетении индуцированных представлений.}
        Пусть
        и
        такие подгруппы в

        что пространства

        компактны. Пусть представления
        и
        конечномерны, а представления
        и
        реализованы в пространствах функций
        и
        соответственно. Пусть

        обобщенная функция на группе

        со значениями в

        и структурным условием

        Тогда сплетающие операторы

        имеют вид

        Доказательство см. в

        {Замечание}.
        Если подгруппа

        некомпактна, то оператор

        может существовать только в виде интеграла по факторпространству

        В силу структурного условия функция

        задана, если известны ее значения на представителях

        двойных классов

        Элементы
        и
        могут совпадать, если

        где
        (см.
        ).
        Следовательно,

        Здесь

        можно считать оператором огрничения

        представления

        на подгруппу

        Иными словами,

        Отметим, что для

        когда

        состоит из одного элемента, в качестве которого удобно выбрать

        функция

        определяется значением в одной точке

        Приведем аналогичный результат для неунимодулярных групп. Пусть

        модули соответствующих групп. Рассмотрим пространство сплетающих
        операторов

        для представлений
        и
        по формулам вида

        Построим дополнительно представление группы

        с операторами

        Свойство

        справедливо для представлений

        если в его формулировке видоизменить структурное условие

        и выражение для оператора

        записывать в виде интеграла по факторпространству

        Здесь функция

        также полностью определяется своими значениями на пространстве

        и должна удовлетворять условию

        В частном случае
        и
        теорему о переплетении можно использовать для вывода критерия
        неприводимости индуцированного представления.

        {Утверждение.}
        Представление

        операторно неприводимо, если

        унитарно, неприводимо и
        (
        для неунимодулярных групп) для всех классов

        кроме тривиального.

        {Доказательство}.
        В данном случае

        унитарное представление. Оно неприводимо, если всякий оператор

        вида

        кратен единичному (см.
        ).
        Для этого достаточно, чтобы представление

        было неприводимо и зависимость

        от

        имела вид

        Действительно,

        Подставим в это выражение

        Для завершения доказательства остается заметить, что

        при

        совпадает с
        и
        для неприводимого представления

        Аналогично доказывается следующее утверждение.

        Неприводимые индуцированные представления

        неэквивалентны, если

        при всех

        Нетрудно переформулировать это утверждение для неунимодулярных
        групп.

        Одним из важнейших результатов в общей теории индуцированных
        представлений является теорема взаимности, впервые доказанная
        Фробениусом для конечных групп.

        {Теорема Фробениуса.}
        Пусть

        представление группы

        унитарное, неприводимое представление подгруппы

        Тогда

        Это свойство представляет собой сокращенный вариант теоремы Макки
        (полную формулировку см. в
        ).

        Остановимся на важном следствии теоремы Фробениуса.

        Пусть группа

        компактна. Напомним, что регулярное представление

        группы

        эквивалентно

        индуцированному тривиальным представлением подгруппы

        Если

        неприводимое представление, то размерность пространства

        совпадает с размерностью оператора

        т. е. пространства

        Теорема Фробениуса позволяет утверждать, что регулярное
        представление содержит любое неприводимое представление

        группы

        с кратностью, равной

        (ср. с
        ).

        {Метод малой группы. Представления группы

        Группа Пуанкаре, ее орбиты. Представления собственной группы
        Пуанкаре для
        и
        Представления общей группы Пуанкаре}

        Процедура построения неприводимых унитарных представлений групп,
        содержащих нетривиальную разрешимую нормальную подгруппу,
        называется методом малой группы. Он был разработан Е. П. Вигнером

        специально для группы Пуанкаре. В пятидесятых годах Г. Макки
        предпринял детальное систематическое исследование индуцированных
        представлений, и в частности метода малой группы, получившего
        широкое распространение в теории групп и квантовой физике.

        Пусть

        связная группа Ли. Тогда по теореме Леви-Мальцева (см. теорему
        )
        где подгруппа

        полупроста, а подгруппа

        разрешима. Пусть

        неприводимое представление нормальной подгруппы. Всякому элементу

        сопоставим представление

        Совокупность преставлений

        называется {орбитой представления}

        Среди

        очевидно присутствуют представления, эквивалентные

        например,

        где

        Выделим совокупность всех таких

        что

        Как легко убедиться, множество

        составляет подгруппу

        Подгруппа

        носит название {малой группы представления}

        Очевидно, что

        содержится в

        как нормальная подгруппа. Факторгруппа

        называется {малой когруппой}.

        Если в качестве исходного представления выбрать любой другой
        элемент

        орбиты

        то его малая группа

        будет сопряжена группе

        Все малые группы представления одной орбиты изоморфны.

        {Теорема.}
        Пусть разложение Леви-Мальцева связной группы

        имеет вид

        Пусть

        неприводимое (одномерное) представление нормальной подгруппы

        Построим его малую группу

        и малую когруппу

        Рассмотрим неприводимое представление

        группы

        и представление

        вида

        Индуцированное представление

        является неприводимым, и тем же методом можно получить всякое
        неприводимое представление группы

        При этом каждой орбите

        взаимно-однозначно сопоставляется класс эквивалентных неприводимых
        представлений группы

        Всякое унитарное представление группы

        можно построить в виде

        с помощью унитарных представлений
        и

        Приведем алгебраическую часть доказательства. Подробное
        доказательство см. в

        Построим представление

        По теореме о подгруппах

        его ограничение

        разлагается в прямой интеграл

        Поскольку

        нормальная подгруппа в

        то

        и все

        эквивалентны унитарному неприводимому представлению

        Свойство

        очевидно справедливо и для всякой неприводимой компоненты

        представления

        Покажем, что индуцированное представление

        неприводимо. Используем утверждение

        в качестве критерия неприводимости. Рассмотрим пространство

        найдем группы

        и построим представления

        Всякая группа

        очевидно содержит нормальную подгруппу

        Тем же свойством обладает и всякая группа

        Для нетривиальных классов

        достаточным условием обращения в нуль пространства

        является отсутствие нетривиальных сплетающих операторов для
        ограничений

        Поскольку для

        справедливо разложение

        то всякое представление

        также разлагается в прямой интеграл

        Представления

        образованы из

        с помощью элементов

        Следовательно, если

        то во множестве

        нет эквивалентных пар представлений. Неприводимость представления

        доказана.

        Итак, неприводимое унитарное представление группы

        может быть индуцировано неприводимой компонентой

        представления

        Покажем, что для представления

        справедлива формула

        Малая группа

        содержит нормальную подгруппу

        Следовательно,

        как и

        представима в виде полупрямого произведения

        Элементы

        подгруппы

        могут быть выбраны в качестве представителей классов

        причем все факторы

        тривиальны (см.
        ).

        Реализуем представление

        на пространстве

        Так как всякий элемент

        представим в виде

        то произвольная функция

        в силу структурного условия полностью определяется своими
        значениями на факторгруппе

        Поэтому достаточно рассмотреть действие операторов
        на
        Представление

        неприводимо и, следовательно, одномерно. По определению малой
        группы представление

        эквивалентно

        и в силу одномерности совпадает с ним, так что

        Отсюда следует, что

        есть оператор умножения на число, в то время как

        осуществляющий левый сдвиг на скалярных функциях

        является оператором левого регулярного представления группы

        так что подпространство в

        неприводимое относительно представления

        неприводимо и относительно

        Иными словами, выделение неприводимой компоненты

        сводится к аналогичной задаче для левого регулярного представления
        группы

        Предположим, что эта задача решена --- неприводимое унитарное
        предмтавление

        построено. Пусть

        неприводимая компонента представления

        такая, что

        Формула

        справедлива и для подпредставления

        Следовательно, опрераторы

        обладают структурой

        Всякое неприводимое представление

        может быть выделено из представления, индуцированного его
        ограничением

        в виде неприводимой компоненты. Мы предлагаем читателю
        самостоятельно провести детальное доказательство этой части
        теоремы.

        {Упражнение}.
        Покажите, что {всякое} унитарное неприводимое представление группы

        представимо в виде

        где

        определяется формулой

        Вновь вернемся к рассмотрению неприводимого представления

        и его ограничения

        Очевидно, что какую бы неприводимую компоненту

        мы ни выбрали для построения операторов

        вида

        индуцированное представление

        будет эквивалентно

        Пусть

        действует в пространстве

        В силу неприводимости

        всякий вектор

        (соответствующий неприводимой компоненте
        )
        представим в виде

        Следовательно, всякое подпредставление

        эквивалентно

        Для получения неприводимых представлений группы

        (из каждого класса эквивалентности) достаточно выбрать по одному
        представлению

        из каждой орбиты неприводимого представления группы

        {Замечание}.
        При доказательстве теоремы мы не использовали полупростоту
        подгруппы

        Утверждения теоремы остаются в силе и тогда, когда группа
        изоморфна полупрямому произведению разрешимой нормальной подгруппы

        и произвольной подгруппы

        Проиллюстрируем метод малой групы простейшим примером. Построим
        неприводимые унитарные представления группы

        В данном случае справедливо разложение Леви-Мальцева

        несмотря на то, что фактор Леви

        оказался абелевой группой.

        Пусть
        и
        двумерные вещественные векторы пространств

        соответственно. Вектор

        параметризует неприводимые унитарные представления группы

        Поскольку подгруппа

        нормальна, ее топологическое пространство

        является

        модулем относительно преобразований подобия

        где

        действует на векторы

        как оператор вращения в определяющем представлении. На дуальном
        пространстве

        реализуется дуальный

        модуль:

        Орбиты группы

        в пространстве

        окружности радиусом

        Множество орбит распадается на два класса:

        Всякой орбите
        в
        соответствует орбита

        представления

        Рассмотрим случай

        Здесь подгруппа стабильности точки (она же малая группа

        представления) совпадает с

        в силу соотношений

        Малая когруппа тривиальна, и представление

        имеет вид

        Индуцированное представление реализуем в пространстве скалярных
        функций на

        где

        играют роль представителей классов

        Перейдем к рассмотрению орбиты с

        В этом случае группа

        целиком составляет малую группу

        (см. формулу
        ).
        Малая когруппа

        изоморфна

        Запишем ее унитарные неприводимые представления в виде

        В силу совпадения малой группы с исходной группой

        искомое индуцированное представление эквивалентно

        Мы построили полную систему неприводимых унитарных представлений
        группы

        В группе Пуанкаре закон композиции определяется умножениями в
        группах

        и автоморфизмами

        Метод малой группы применим для связных групп Ли. Поэтому
        конструирование представлений группы Пуанкаре начнем с построения
        неприводимых представлений ее связной компоненты --- собственной
        ортохронной группы Пуанкаре

        В гл. 8 было установлено, что нам необходимы унитарные проективные
        представления группы Пуанкаре и что они эквивалентны векторным
        унитарным представлениям, ее универсальной накрывающей. Здесь и
        далее для краткости квантовомеханическую группу Пуанкаре будем
        обозначать буквой {П:}

        Рассмотрим группу {П} как {П}-модуль относительно
        преобразований подобия. Тогда нормальная подгруппа

        также приобретает структуру {П}-модуля. Очевидно, что сама
        подгруппа

        действует на

        тривиально. Действие

        на

        соответствует автоморфизмам

        Выпишем явный вид преобразований пространства

        группы

        и дуального ему пространства

        со структурой дуального {П}-модуля:

        модуль

        эквивалентен

        модулю

        Классификация орбит группы

        на

        совпадает с классификацией орбит

        на

        (см.
        )
        В указанных

        однородных топологических пространствах подгруппами стабильности
        стандартного вектора являются
        для
        для
        (двукратная накрывающая группы
        ) --- для
        и
        и, наконец, сама

        для

        Поскольку подгруппа

        тривиально действует на векторы

        то подгруппами стабильности

        однородных пространств

        являются:

        стандартный вектор

        стандартный вектор

        стандартный вектор

        Приступим к построению неприводимых представлений группы

        Запишем неприводимое унитарное представление группы

        в виде

        Здесь вектор

        нумерующий представления, очевидно принадлежит дуальному

        модулю

        Отметим, что в представлении

        всякий генератор группы трансляций

        реализуется оператором умножения на число --- компоненту вектора

        Построим орбиту представления

        Соотношение

        означает, что множество неприводимых представлений

        нумеруется параметрами

        принадлежащими одной орбите группы Пуанкаре в пространстве

        Пусть

        Выберем исходное представлениеъ

        с индексом

        (стандартным вектором
        ).
        Тогда из формулы

        следует, что малая группа

        представления

        изоморфна подгруппе стабильности стандартного вектора однородного
        пространства

        а малая когруппа

        совпадает с

        Полная система неприводимых унитарных представлений

        группы

        была построена в

        С помощью операторов

        по формуле

        построим неприводимое унитарное представление

        Неприводимое унитарное представление группы Пуанкаре

        реализуем в пространстве

        интегрируемых с квадратом функций
        на
        со значениями в пространстве

        представления

        Формула

        позволяет отождествить пространство

        с пространством

        представления

        так как

        В свою очередь факторпространство

        изоморфно орбите

        (или
        )
        (см. классификацию
        ).

        Как отмечалось в

        всякое представление, индуцированное с

        эквивалентно

        Следовательно, множество

        состоит из представлений группы Пуанкаре, эквивалентных

        т. е. индуцированное представление надлежит снабдить индексами

        (или для краткости
        ).

        Итак, рассмотрим индуцированное представление

        реализованное в пространстве функций на

        со значениями в

        В качестве представителей классов

        входящих в аргумент оператора

        удобно выбрать бусты

        эрмитовы матрицы из

        такие, что

        (см
        ).
        Преобразуем аргумент оператора

        используя формулы

        и определение факторов

        Подставим полученное выражение в формулу

        и упростим аргумент функции

        в соответствии со структурой

        модуля

        Функции

        описывают состояния элементарной релятивистской квантовой частицы.
        Каждому пространству

        неприводимого представления

        можно приписать индексы

        и, таким образом, классифицировать элементарные частицы по их
        пространственно-временным характеристикам. Какав физический смысл
        этой классификации?

        Напомним, что вектор

        в формуле

        реализует генератор трансляции в представлении

        Аргумент функции состояния

        принадлежит

        как факторпространству

        Удостоверимся, что и здесь вектор

        имеет смысл импульса частицы. Подействуем на функцию

        оператором

        Отметим, что собственное значение оператора импульса в
        представлении

        совпадает с аргументом функции состояния. Векторы

        модуля

        имеют физический смысл импульсов релятивистских квантовых систем.
        Инвариант

        с помощью которого классифицируются орбиты модуля

        очевидно есть масса частицы.

        Для расшифровки значения индекса

        достаточно рассмотреть действие оператора

        на вектор состояния частицы в системе покоя:

        Полученный закон преобразования означает, что функция

        описывает частицу со спином

        Итак, классификация элементарных частиц по свойствам симметрии
        пространства-времени производится по спину, массе (и закону
        энергиии).

        {Замечание}.
        Множество векторов

        можно в известном смысле считать волновой функцией релятивистской
        квантовой частицы. При этом набор наблюдаемых ограничен
        пространственной симметрией и предполагается, что

        задана на спектре из значений, причем

        отлична от нуля лишь при
        и
        равных индексам представления

        Индекс

        связан с собственным значением оператора Казимира

        алгебры Пуанкаре (см.
        )
        формулой

        {Упражнение}.
        Докажите соотношение

        Представление

        унитарно относительно скалярного произведения вида

        Здесь

        инвариантная мера на гиперболоиде

        (см. упражнения
        ):

        в подинтегральном выражении 4-импульс

        имеет вид

        В формуле

        оператор представления

        записан так, что функция состояния преобразуется как спинор только
        в системе покоя. Для выявления спинорных свойств

        при любом значении импульса

        выполним преобразование базиса в пространстве

        Предварительно перепишем выражение для оператора представления:

        Дополним представление

        до представления

        группы

        (см.
        ),
        которое действует в том же пространстве

        Ограничение

        совпадает с

        Следовательно, в формуле

        оператор

        можно рассматривать как ограничение на

        соответствующего оператора представления группы

        Перейдем к новым базисным функциям

        Тогда оператор индуцированного представления имеет вид

        В этой формуле отчетливо проявляются свойства функции

        как релятивистского спинора. Базис

        носит название {спинорного базиса}.

        Перепишем скалярное произведение

        в терминах спинорных функций состояния:

        При выводе этой формулы мы воспользовались свойствами матриц Паули
        и бустов в группе

        (см.
        ).

        Проведенное выше построение для орбит

        практически без изменений переносится на орбиты

        (с той же группой стабильности), так что формулы
        и
        описывают преобразования состояний релятивистских элементарных
        квантовых частиц с массой покоя

        и спином

        как в случае положительной, так и отрицательной энергии.
        Представления, соответствующие орбитам

        (т. е. частицам с мнимой массой и частицам с импульсом,
        тождественно равным нулю) не играют существенной роли в квантовой
        физике. Остается рассмотреть орбиты
        и
        с подгруппой стабильности

        для стандартных векторов
        и

        Пусть в представлении

        (см. формулу
        )
        стандартный вектор имеет вид

        Тогда с помощью соотношений
        и
        можно найти структуру малой группы и малой когруппы:

        Полная система неприводимых унитарных представлений группы

        была построена в

        Поскольку группа

        является двукратным накрытием собственной группы Пуанкаре, то
        подгруппа стабильности стандартного вектора
        в
        также двукратная накрывающая соответствующей подгруппы
        стабильности в

        Для представлений первого класса (см. формулу
        )
        переход к накрывающей группе

        означает появление нетривиальной малой когруппы

        В этом случае (при
        )
        представления

        бесконечномерны. Следовательно, индуцированное представление

        должно быть реализовано в пространстве функций с бесконечным
        числом компонент, т. е. бесконечнокомпонентных спиноров.
        Элементарные частицы с такими свойствами не обнаружены, и мы не
        будем подробно рассматривать эти представления.

        В случае

        при переходе к группе

        малая когруппа также двукратно накрывается. Обозначим ее

        Соответственно изменяются области значения параметров
        представлений
        и

        Построим представление

        и индуцируем представление группы Пуанкаре

        Здесь функции

        задаются на пространстве

        Бусты

        определены равенством

        Под действием операторов, соответствующих в группе

        вращениям вокруг третьей оси, функция состояния в системе
        стандартного импульса преобразуется как объект со спиральностью

        Представление

        унитарно относительно скалярного произведения (см.
        ):

        Существенные особенности представлений с

        проявляются при построении спинорного базиса. Представление

        неточно, его ядро нетривиально. Следовательно, у простой группы

        не может быть представлений, ограничения которых на

        совпадают с

        Тем не менее, спинорный базис необходим нам для построения
        представлений группы Пуанкаре с отражениями. Откажемся временно от
        унитарности представления, чтобы ввести спинорный базис. Построим
        вспомогательное представление

        со следующими свойствами: 1) точное; 2) содержащее
        подпространство унитарного представления
        ;
        3) имеющее пространство представления, на котором реализуется
        представление

        группы

        (желательно неприводимое), причем ограничение

        эквивалентно

        Тем самым практически однозначно определяется конечномерное,
        неунитарное

        мерное представление

        Далее в представлении

        спинорный базис вводится стандартно:

        Еще раз подчеркнем, что представление

        неунитарно.

        Процедура построения представления группы Пуанкаре для частиц со
        спиральностью

        нулевой массой покоя и положительной энергией почти полностью
        повторяется при построении представлений с отрицательной энергией
        (орбита
        ).

        Обратимся к представлениям общей группы Пуанкаре

        где

        общая группа Лоренца, содержащая подгруппу отражений

        и нормальную подгруппу

        В определяющем представлении группы

        операторы отражений в пространстве

        задаются очевидными равенствами (см. формулы
        и)

        При расширении группы

        операторами отражения квадраты их уже не обязательно должны
        совпадать с единицей, так что можно построить несколько
        неэквивалентных расширений

        каждое из которых при факторизации по

        переходит в

        Рассмотрим полупрямое произведение

        Оно содержит подгруппы
        и
        являющиеся

        модулями относително канонических автоморфизмов

        На векторном пространстве

        естественно возникает структура дуального

        модуля (относительно формы
        ).
        Однако такой, внешне очевидный, путь реализации преобразований

        на пространстве

        приводит к противоречию с экспериментально наблюдаемой
        положительностью энергии физических частиц. Для сохранения
        положительности энергии мы вынуждены отказаться от реализации

        модуля

        как дуального к

        Напомним, что преобразования симметрии квантовой системы могут
        быть представлены либо унитарными, либо антиунитарными
        операторами. Оператор пространственного отражения не влияет на
        знак энергии, поэтому предположим, что антиунитарными (и,
        следовательно, антилинйными) являются операторы отражения времени

        и полного отражения

        Воспользуемся формулами
        и

        При сохранении знака энергии под действием антилинейного оператора

        импульс преобразуется по правилу

        Аналогично получаем закон преобразования

        под действием
        и
        отражений:

        Для нахождения соответствующих формул для

        как

        модуля воспользуемся свойством накрывающего отображения и
        гомоморфностью групп
        и
        Свойства

        остаются справедливыми и для группы Лоренца, операторы которой в
        данном случае удобно параметризовать элементами орбит и подгруппы
        стабильности точки. Разложим произвольную матрицу

        группы Лоренца в определяющем представлении на произведение
        симметричной и ортогональной матриц:
        (см.
        ),
        подставим в нее выражение

        и воспользуемся явным видом оператора

        в определяющем представлении:

        При переходе к накрывающей группе

        симметричной матрице

        сопоставляется эрмитова

        а ортогональной

        унитарная

        Легко убедиться, что равенство вида

        сохраняет силу и для группы

        Поскольку конечномерные представления

        группы

        обладают свойством

        (см. упражнение
        ),
        то и для них справедливо правило

        Операторы в правой части равенства принадлежат сопряженному
        представлению. Если

        самосопряженное представление

        то оператор

        может быть определен в пространстве

        В противном случае для задания

        необходима прямая сумма пространств
        и
        Для неприводимого представления

        соотношение

        принимает вид

        Отметим, что операторы

        инвариантны относительно действия отражения

        Последнее непосредственно вытекает из соотношений

        Если в пространстве

        неприводимое представление

        действует в спинорном базисе по формуле

        то в пространстве векторов

        действие группы Пуанкаре задается операторами

        где

        оператор левого сдвига. Следовательно, область значений функции

        принадлежит пространству

        представления

        Для построения оператора

        необходимо рассмотреть пространство

        функций со значениями в прямой сумме

        Полученный таким образом

        модуль в общем случае является приводимым.

        Перейдем к рассмотрению оператора отражения времени. Представим
        антилинейный оператор

        в виде произведения оператора комплексного сопряжения и линейного
        оператора

        и перепишем с их помощью автоморфизм

        Пользуясь явным видом оператора

        в определяющем представлении группы

        получаем окончательно

        Свойство операторов конечномерных представлений

        (см. упраженение
        )
        позволяет выразить матрицу

        через оператор

        где учтена унитарность оператора

        Предположим, что неприводимое проективное представление

        построено (причем операторы
        и
        унитарны, а

        антиунитарен). Ограничение

        в общем случае является приводимым проективным унитарным
        представлением группы

        Разложим представление

        на неприводимые. Очевидно, что всякая неприводимая компонента
        эквивалентна неприводимому унитарному представлению группы

        Пространство такого неприводимого представления при

        (см.
        )
        есть

        где

        пространство неприводимого представления

        группы

        на котором реализуется представление

        группы

        Рассмотрим вектор

        и подействуем на него операторами
        и
        Как уже было показано, эти операторы осуществляют преобразование

        так что пространство неприводимого представления

        содержится в

        Само представление

        также эквивалентно некоторому неприводимому унитарному
        представлению одной из накрывающих групп

        Всякая группа

        представляет собой двукратное накрытие группы

        так что квадраты операторов

        удовлетворяют условиям

        Следовательно, имеется восемь неэквивалентных групп

        и соответственно восемь типов неприводимых квантовомеханических
        представлений

        {Упражнение}.
        Покажите, что во всяком представлении

        удовлетворяющем перечисленным выше свойствам, таблица умножения
        ограничения

        эквивалентна табл. 13.

        Начнем построение представления

        с оператора

        Обратим внимание на тот факт, что подпространство функций от

        инвариантно относительно действия оператора пространственного
        отражения. На этом подпространстве оператор

        должен выглядеть особенно просто, так как преобразуется только
        форма функции

        Всякий вектор

        может быть представлен в виде пары

        где

        а

        преобразуется по представлению

        (см.
        ).
        Подействуем оператором

        на вектор

        и воспользуемся соотношением

        В том же базисе оператор

        имеет вид

        Как было установлено (см. равенства
        и
        ),
        оператор
        и
        коммутируют. Следовательно, операторы
        и
        кратны единичному (см.
        ):

        Унитарность оператора

        позволяет фиксировать модули чисел
        и
        Наконец, в соответствии с выводами

        мы должны рассмотреть две возможности:

        {Упражнение}.
        Покажите, что всякое проективное представление группы

        в первом случае эквивалентно представлению с

        либо

        во втором --- представлению, где

        либо

        Таким образом, в пространстве всякого квантовомеханического
        представления

        возможен переход к такому базису, где векторы состояний

        в системе покоя --- собственные векторы оператора

        Их собственные значения будут равны

        в первом и

        во втором случаях. Отметим, что в силу линейности операторов

        всякий вектор

        также будет собственным с тем же собственным значением. При
        наличии

        симметрии должен существовать суперотбор по квантовому числу

        четности.

        Мы установили явный вид оператора

        на инвариантном подпространстве векторов

        Чтобы найти выражение для

        на всем пространстве

        воспользуемся соотношениями

        Оператор

        отражения принимает наиболее простой вид на пространстве

        Если представление

        действует в когерентном пространстве состояний с фиксированной
        четностью

        то выражение

        для скалярного произведения в спинорном базисе можно переписать,
        используя оператор

        При выводе этого выражения мы использовали основное свойство
        оператора

        отражения (см. формулу
        ).

        Чтобы завершить построение представления

        выпишем явный вид оператора

        В соответствии с табл. 13

        так, что

        Так же строятся и представления

        ограничения которых на

        эквивалентны представлениям

        с массой нуль.

        В теории поля происходит дальнейшее уточнение величин
        и
        в формулах
        и
        Собственные значения оператора

        отражения оказываются равными

        для частиц с целым спином и

        для частиц с полуцелым спином. Далее, с учетом внутренней
        симметрии группа Пуанкаре расширяется с помощью дискретной
        операции зарядового сопряжения. Связь между фазовыми множителями
        во всех четырех операциях отражения фиксируется теоремой о

        инвариантности

        Пространства представлений

        вида

        приводимы за исключением

        По построению представление

        неприводимо. Следовательно, указанные пространства характеризуются
        собственными значениями операторов Казимира группы

        Как мы выяснили, вместо

        следует использовать значение фазового множителя

        четность частицы. Мультипликаторы
        и
        оказываются связанными со спином частицы. Действительно,

        Так как

        то

        Вследствие

        инвариантности третья фаза

        оказывается однозначно связанной с зарядовой четностью, так что
        представление

        фиксируется заданием
        и
        четностей частицы.

        {Релятивистские уравнения движения. Волновые функции, неприводимые
        представления и ковариантные проекторы. Методы построения
        уравнений движения. Примеры}

        Основной задачей теории элементарных частиц является построение
        модели их взаимодействия. Локальность взаимодействия элементарных
        частиц приводит к необходимости определить состояние объекта в
        заданной точке пространства Минковского. Переход от импульсного
        представления к координатному осуществляется преобразованием Фурье
        (см., например,
        ).
        В предыдущем пункте мы построили некоторые, в общем случае
        приводимые, представления

        Трехмерное преобразование Фурье от элементов пространств
        и
        на которых реализованы операторы

        определяет координатное представление в первично квантованной
        теории, где зависимость от времени следует из уравнения
        Гейзенберга и для свободной частицы описывается множителем

        Описание процессов рождения и уничтожения частиц требует
        локальности и во времени. Для того чтобы стало возможным
        преобразование Фурье по переменной

        необходимо вложить пространство

        Элементы пространства

        обычно называют {волновыми функциями} (ср.
        ).

        Предположим, что удалось построить проектор

        который выделяет в пространстве

        неприводимое подпространство представления общей группы Пуанкаре.
        Оно состоит из решений уравнения

        Проектор

        в координатном представлении есть функция от оператора

        Соотношения
        и
        называются {уравнениями движения} релятивистской свободной
        элементарной частицы в импульсном и координатном представлениях

        Используя свойства пространств
        и
        можно утверждать, что решения уравнений
        и
        описывают состояния частицы с массой

        спином

        положительной энергией, фиксированной

        четностью и фазой

        Значок

        в проекторе

        в дальнейшем опускаем.

        Обратим внимание на две существенно различные причины приводимости
        пространства

        замену пространства

        неприводимого представления малой группы пространства

        или

        и переход от пространства функций на орбите

        к пространству функций на

        Проектор

        должен устранить приводимость как первого, так и второго рода.
        Различие будет особенно заметным при сравнении уравнений движения
        бесспиновых частиц и частиц со спином

        Представление

        действующее на пространстве

        неприводимо. Проектор, выделяющий это пространство в

        имеет очевидную структуру:

        В координатном представлении это уравнение совпадает с {уравнением
        Клейна-Гордона}.

        Рассмотрим представление

        в пространстве

        Из явного вида операторов

        (см. формулу
        )
        следует, что подпространство векторов вида

        инвариантно относительно действия оператора пространственного
        отражения. Формулы
        и
        позволяют убедиться в инвариантности и неприводимости относительно
        действия всей группы

        Более того, пространство векторов вида

        описывает частицу с фиксированным спином

        так как в системе покоя функция состояния под действием операторов
        группы

        преобразуется по неприводимому представлению, эквивалентному

        Таким образом, искомый проектор

        на подпространстве векторов в системе покоя совпадает с
        проектором, выделяющим подпространство

        неприводимого представления

        Нетрудно получить выражение для

        в произвольной системе отсчета. Пусть

        Тогда

        где

        Введем обозначение

        и воспользуемся стандартным разложением

        Проектор

        сплетает представления группы

        т. е. операторы
        и
        коммутируют. Следовательно,

        {Упражнение}.
        Покажите, что

        ковариантный оператор:

        Перейдем к пространству функций

        Оператор

        должен не только проектировать область значений функции на
        неприводимое подпространство

        но и гарантировать принадлежность импульса частицы фиксированной
        орбите

        Для параметра

        в проекторе

        это условие выполнено (если
        ).
        Таким образом, функция

        только тогда является решением уравнения

        когда ее аргумент принадлежит орбите

        Итак, множество решений уравнения

        составляет пространство неприводимого представления

        Соотношение

        является общим релятивистским ковариантным уравнением движения для
        массивной частицы со спином. Конкретный вид проектора

        зависит от выбора пространства

        в которое погружается пространство неприводимого представления
        собственной квантовомеханической группы Пуанкаре. Далее мы
        рассмотрим стандартные формы уравнений движения.

        Специфика конструкции спинорного базиса в пространстве состояний
        безмассовых частиц не позволяет полностью распространить
        приведенные выше рассуждения на случай

        (см.
        и).
        {Уравнение Дирака.}
        Построим пространство неприводимого предсталения

        Для этого рассмотрим пространство волновых функций

        Чтобы волновые функции в системе покоя преобразовались по
        неприводимому представлению группы вращения со спином

        достаточно использовать проектор на одно из прямых слагаемых в
        пространстве

        Здесь

        диагональна, т. е.

        Произведя замену

        с помощью определения

        и соотношений

        найдем вид проектора

        Подставив выражение

        в общее уравнение движения

        получим {уравнение Дирака}

        Каждая компонента рашения

        уравнения Дирака удовлетворяе уравнению Клейна-Гордона, так как
        условие

        эквивалентно требованию

        Для скалярного произведения в пространстве волновых функций Дирака
        удобно использовать выражение

        В представлении, где операторы

        приведены к блочно-диагональному виду, для оператора

        справедливо выражение

        т. е. он совпадает с матрицей

        где введена операция дираковского сопряжения

        {Уравнение Прока.}
        Для частицы со спином

        в качестве исходного неприводимого представления группы

        можно рассмотреть

        и получить пространство функций

        Если же начать рассмотрение с приводимого представления

        на пространстве

        то при переходе к спинорному базису получим представление

        группы

        Оператор

        отражения не выводит из пространства

        так что в качестве пространства представления

        можно выбрать

        Воспользуемся явным видом оператора

        в определяющем представлении группы

        Поектор

        (см.
        )
        выделяет состояния частицы со спином

        в системе покоя. Выражение для оператора

        легко получить, используя явный вид бустов в представлении

        определяющем представления группы Лоренца (см.
        ):
        Использование выражения

        в соотношении

        приводит к уравнению движения

        Его решения описывают состояния частицы со спином

        и массой

        Действительно, условие

        приводит к тому, что каждая компонента

        волновой функции удовлетворяет уравнению Клейна-Гордона

        Домножим обе части уравнения

        на компоненту

        импульса, принадлежащего орбите

        и свернем индексы. Использовав условие

        получим соотношение

        которое называется {уравнением Прока}. Множество решений системы
        уравнений
        и
        (или множество решений уравнения Прока с условием
        )
        составляет пространство неприводимого представления

        общей группы Пуанкаре. Оператор

        в этом представлении очевидно пропорционален соответствующему
        оператору в определяющем представлении группы Лоренца:

        С его помощью можно получить выражение для скалярного произведения
        в пространстве волновых функций Прока:

        {Уравнения для тензорных волновых функций.}
        Формализм Прока без труда обобщается на волновые функции массивных
        тензорных частиц. Рассмотрим в качестве примера случай

        В приводимом представлении

        при построении спинорного базиса используется представление

        группы

        Его можно реализовать на пространстве симметричных бесследовых
        тензоров ранга 2 (см. упражнение
        ).
        Соответствующим пространством волновых функций является

        Выберем проектор

        вида

        Оператор

        в каждом тензорном сомножителе в

        выделяет подпространства

        так что симметричный бесследовый тензор, удовлетворяющий условию

        преобразуется по представлению

        Оператор

        строится аналогично проектору

        Окончательно уравнения движения для волновых функций частиц с
        массой

        и спином 2 имеют вид

        Полученный результат легко обобщается на случай произвольного
        целого спина

        Выражение для скалярного произведения волновых функций получается
        из формулы

        тривиальной заменой индекса

        мультииндексом

        {Уравнения Рариты-Швингера.}
        Для описания пространств состояний массивных частиц с полуцелым
        спином

        объединим методы, использованные в уравнениях Дирака и Прока. В
        качестве исходного представления рассмотрим тензорное произведение

        В спинорном базисе будет действовать представление

        группы

        которое при переходе к общей группе Пуанкаре необходмо расширить
        до

        Таким образом, пространство волновых функций для састиц со спином

        оказывается равным

        Выберем проектор

        в виде тензорного произведения операторов
        и

        На волновых функциях

        (со значениями в пространстве
        )
        оператор

        в произвольной системе отсчета имеет вид

        Подставив его в условие

        получим систему {уравнений Рариты-Швингера}:

        Пространство ее решений приводимо. Если на функции

        не накладывается дополнительных условий кроме симметризации
        тензорных индексов, проектор

        выделяет в системе покоя подпространство, преобразующееся по
        приводимому представлению

        группы

        Для того чтобы решения уравнений Рариты-Швингера составляли
        пространство неприводимого представления

        в качестве пространства волновых функций следует взять

        С этой целью функция

        записывается в виде спинтензора

        тензорные индексы заменяются парой спинорных индексов, все
        пунктирные и непунктирные спинорные индексы симметризуются (см.
        ).

        {Уравнения Баргмана-Вигнера.}
        Волновые функции для частиц со спином

        можно строить, непосредственно обобщая метод Дирака. Исходное
        представление выберем в виде симметризованного тензорного
        произведения

        экземпляров

        Ему будет соответствовать пространство волновых функций

        с элементами

        где

        биспинорный индекс. Проектор

        строится как тензорное произведение

        операторов

        так что

        Подставив выражение

        в уравнение

        получим систему {уравнений движения Баргмана-Вигнера}:

        Эти соотношения выделяют в пространстве волновых функций
        подпространство неприводимого предсталения

        {Уравнения для

        компонентной волновой функции.}
        То же, что и в предыдущем случае, пространство

        можно рассматривать как множество

        компонентных волновых функции

        Вместо проектора

        действующего на каждый биспинор, построим проектор на пространстве

        в целом, причем инвариантное подпространство будем выделять в виде
        диагонали:

        Как и в уравнении Дирака, первое слагаемое в проекторе

        с точностью до фазового множителя

        совпадает с оператором

        отражения в пространстве

        Построим проектор для функции в произвольной системе отсчета по
        стандартному рецепту

        При выводе этой формулы были использованы соотношение

        и свойство

        Вернемся к базису симметризованных спиноров
        (см.
        ),
        т. е. запишем

        компонентную волновую функцию в виде

        В этом базисе операторы

        предствлены в виде тензорного произведения операторов
        и
        (см.
        ):
        Подставляя полученные выражения в формулу

        приходим к следующим уравнениям движения для частицы со спином

        Легко убедиться, что при

        вновь имеем обычное уравнение Дирака.

        {Неприводимые представления и уравнения движения для частиц с
        .}
        В рассматриваемом случае необходимо иметь в виду следующие два
        обстоятельства.

        Во-первых, не для всех безмассовых частиц экспериментально
        наблюдается симметрия относительно общей группы Пуанкаре

        (см. гл. 8). Пространство состояний нейтрино оказывается
        инвариантным относительно дискретных операций

        отражения и комбинированного

        отражения (см.
        ).
        Построение соответствующих представлений не сопровождается
        характерным для

        отражения `` удвоением '' пространства представления. В частности,
        волновые функции нейтрино --- двухкомпонентные спинорные функции.

        Прежде чем обсуждать вторую особенность представлений с

        напомним, что генератор подгруппы

        малой когруппы

        имеет вид

        (при

        он совпадает с
        ).
        Его собственные значения (спиральность) нумеруют неприводимые
        представления

        группы

        В соответствии с формулами
        и
        оператор спиральности антикоммутирует с оператором

        отражения. Отсюда следует, что элементарная частица с
        фиксированной спиральностью неинвариантна относительно
        пространственного отражения, так что в пространстве неприводимого
        представления

        при

        должны присутствовать как состояния со спиральностью

        так и состояния со спиральностью

        Ограничение

        является (при
        )
        приводимым представлением, в то время как для частиц с

        соответствующее представление

        было неприводимым. При переходе от
        к
        спиральность перестает быть характеристикой безмассовой
        элементарной частицы, напротив, спин (или абсолютная величина
        спиральности) сохраняет значение квантового числа при
        классификации массивных частиц по представлениям группы

        {Уравнение движения нейтрино.}
        В данном случае достаточно рассмотреть пространство неприводимого
        квантовомеханического представления собственной группы Пуанкаре.
        Такое предсталение для

        было построено в предыдущем параграфе (см. формулу
        )
        на функциях

        Перейдем к пространству

        волновых функций в спинорном базисе (см. формулу
        ).
        Наша задача состоит в выделении подпространства в

        инвариантного относительно представления

        т. е. подпространства состояний с массой нуль и спиральностью

        Если фиксировать стандартный импульс

        то операторы алгебры Ли подгруппы

        в группе

        в базисе

        имеют вид

        Поскольку ограничение

        должно быть неточным представлением группы

        то на волновые функции

        в системе стандартного импульса необходимо наложить условия

        Расширим поле скаляров в алгебре

        группы

        Перейдем в

        к базису

        в котором

        и перепишем соотношения

        Эти условия эквивалентны следующим:

        Возвращаясь к базису

        получаем уравнение

        так что проектор

        оказывается равным

        Переход от стандартного импульса

        к произвольному

        произведен с помощью буста:

        В случае малой группы

        всякий буст

        представим в виде произведения

        где множитель

        соответствует гиперболическому повороту в плоскости

        а

        принадлежит подгруппе

        и соответствует трехмерному вращению стандартного вектора

        Поскольку элементы

        коммутируют, то проектор

        можно записать в виде

        где

        так как

        Проектор

        выделяет в пространстве волновых функций

        подпространство состояний элементарной частицы с массой

        и спиральностью

        Решения уравнения движения

        образуют пространство неприводимого квантовомеханического
        представления

        собственной группы Пуанкаре. В частном случае

        уравнение

        носит название {уравнения Вейля} для нейтрино.

        {Уравнение Максвелла.}
        В спинорном базисе пространство волновых функций системы,
        инвариантной относительно общей группы Пуанкаре, как и в случае

        можно выбрать в виде

        Существенно, что здесь задача состоит в построении проектора на
        состояния со спиральностями
        и
        Неприводимые квантовомеханические представления группы

        нумеруются индексом

        Воспользуемся вычислениями, проведенными для нейтрино, заменяя
        всякий раз представление

        прямой суммой

        Представим волновую функцию

        в виде пары

        с областью значений

        для
        и
        для

        Тогда из условий вида

        получим

        Вместо уравнения

        в базисе

        имеем

        Дальнейшие выкладки, аналогичные

        приводят к системе уравнений движения

        решения которой образуют пространство неприводимого представления

        В частном случае

        получаем

        Напомним, что
        и
        трехмерные комплексные векторы. В трехмерных обозначениях
        уравнения примут вид

        Полученную систему легко привести к виду, знакомому из
        электординамики, заменив комплексные поля
        и
        на действительные
        и
        Так как

        удовлетворяет тому же уравнению, что и

        то положим

        В результате уравнения

        превращаются в {уравнения Максвелла} для электромагнитного поля в
        вакууме

        Ограничение

        является приводимым унитарным представлением собственной группы
        Пуанкаре. Полезно написать выражение для скалярного произведения
        на пространстве решений системы

        в спинорном базисе. В соответствии с формулами
        и
        имеем

        Подставим произведение

        в предыдущую формулу и воспользуемся эрмитовостью матрицы

        Здесь представление

        прямая сумма

        а элемент

        записывается в виде матрицы

        Поскольку решения уравнений

        имеют лишь два значения спиральности:
        и
        то окончательное выражение для скалярного произведения принимает
        вид

        Представление

        для электромагнитного поля можно реализовать на пространстве
        функций

        со значениями в антисимметризованном тензорном произведении

        двух четырехмерных векторных пространств. Никаких принципиальных
        изменений, кроме некоторого усложнения проекционных операторов,
        при этом не происходит. Такая же ситуация возникла при
        рассмотрении массивных частиц, где одно и то же неприводимое
        представление реализовалось различными проекторами на различных
        пространствах волновых функций, инвариантных относительно действия
        группы

        и содержащих искомое неприводимое подпространство. Выбор той или
        иной формы уравнений движения диктуется условиями конкретной
        физической задачи.

        Для применения рассмотренного формализма к описанию реальных
        физичкских частиц следует построить полную группу физической
        симметрии

        На примере формул масс

        можно видеть, что пространственно-временная и внутренняя симметрия
        нетривиально связаны. Истинная природа этой связи пока не
        известна. В качестве первого приближения естественно считать
        группу

        равной прямому произведению групп Пуанкаре и внутренней симметрии

        Тогда пространство свободной частицы можно описать решениями
        уравнений движения

        снабдив волновые функции индексом внутренней симметрии, нумерующим
        базисные векторы неприводимого представления группы

        Описание процессов взаимодействия элементарных частиц требует
        перехода к формализму вторичного квантования --- квантовой теории
        поля. В ней волновым функциям, рассмотренным в

        сопоставляются операторы поля

        которые по-прежнему удовлетворяют свободным уравнениям движения

        в представлении взаимодействия.

