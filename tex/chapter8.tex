
        ГЛАВА 8

        СИММЕТРИЯ В КВАНТОВОЙ ФИЗИКЕ. ЭЛЕМЕНТАРНЫЕ ЧАСТИЦЫ

        {Квантовомеханическое описание и преобразования симметрии. Теорема
        Вигнера и проективность представления группы симметрии.
        Унитарность. Элементарные частицы и неприводимые представления}

        Теория элементарных частиц основывается на квантовомеханическом
        описании физических явлений.

        Основные аксиомы квантовой механики предполагают, что состоянию
        физической симметрии системы соответствует вектор

        (вектор состояния) гильбертова пространства

        а наблюдаемой

        --- оператор

        в этом пространстве. Принцип суперпозиции приводит к необходимости
        сопоставления наблюдаемым линейных операторов.

        Наблюдаемой

        соответствует некоторый прибор для ее измерения. С
        квантовомеханической точки зрения роль прибора активна. В
        процессе измерения, как правило, создается новое состояние, такое,
        что повторное измерение этой наблюдаемой (если оно возможно)
        должно дать тот же результат. Поэтому предполагается, что для
        каждой наблюдаемой

        должны существовать собственные состояния ее оператора

        и результатом любого измерения наблюдаемой

        в любом состоянии

        может быть лишь число, принадлежащее спектру

        В разложении произвольного состояния

        по набору нормированных собственных состояний

        (или просто
        )
        наблюдаемой

        коэффициенты

        интерпритируются как {амплитуды вероятности} того, что

        совпадает с

        (Сама вероятность равна
        .)
        Функция

        на спектре наблюдаемой

        называется {волновой функцией} состояния

        Набор наблюдаемых, значениями которых можно характеризовать
        состояния, ограничен требованием одновременной измеримости этих
        наблюдаемых. Таким наблюдаемым

        соответствуют взаимно коммутирующие операторы

        Следовательно,

        обладают общей системой собственных состояний

        которые могут быть пронумерованы с помощью собственных чисел

        операторов

        Если минимальный набор

        назависимых наблюдаемых содержит лишь коммутирующие наблюдаемые и
        любая наблюдаемая, не содержащаяся в

        и коммутирующая со всеми наблюдаемыми набора

        есть функция последних, то он называется {полным набором}.

        {Замечание}.
        Напомним, что различным способам выбора полного набора
        соответствует разнообразие {представлений} (волновых функций) в
        квантовой механике.

        {Замечание}.
        Разложение

        следует тогда понимать как разложение по системе общих собственных
        состояний наблюдаемых полного набора.

        Нормируемым состояниям соответствуют квадратично суммируемые
        волновые функции на спектре наблюдаемых полного набора. Их
        множество и представляет собой квантовомеханическое фазовое
        пространство. (В действительности оно несколько шире, поскольку
        приходится для удобства включать в него собственные состояния
        основных наблюдаемых, скажем,

        или

        принадлежащие непрерывному спектру и нормируемые только в
        обобщенном смысле.)

        Описание квантовомеханических состояний с помощью векторов

        гильбертова пространства нуждается в одном существенном уточнении.
        Предсказаниями теории являются вероятности переходов

        и математрические ожидания

        (средние значения) наблюдаемых

        Отсюда следует, что векторы

        приводят к одним и тем же предсказаниям для всех наблюдаемых и,
        таким образом, соответствуют одному и тому же состоянию объекта,
        т. е., строго говоря, физические состояния в квантовой механике
        описываются не векторм

        а {лучом} --- совокупностью векторов

        Вектор состояния
         ---
        только представитель класса векторов, соответствующих данному
        состоянию. Это обстоятельство, как мы увидим, приводит к отличию
        квантовомеханической симметрии от классической.

        Обратимся к преобразованиям симметрии в квантовой физике (ср.
        ).
        Будем считать их активными, т. е. преобразованиями множества
        состояний

        Тогда преобразованию соответствует некий оператор, который мы
        обозначим через

        и новое состояние запишем так:

        Определяющее свойство преобразований физической симметрии (см.
        )
        можно теперь сформулировать как перестановочность (с точностью до
        фазового множителя!)

        с оператором развития

        Как и прежде, отсюда следует, что совокупность преобразований
        симметрии является группой.

        {Замечание}.
        Следует различать приборы, создающие состояния, и собственно
        измерительные, используемые для наблюдения за развитием состояний.

        Динамика исходного и преобразованного состояний прослеживается по
        временной зависимости средних значений наблюдаемых в этих
        состояниях. Соответствующие измерения можно мыслить как проводимые
        одним наблюдателем одним и тем же набором измерительных приборов.
        Таким образом, активное преобразование есть преобразование только
        векторов

        а операторы, описывающие наблюдаемые, при этом не меняются.

        Иное положение возникает при рассмотрении пассивных
        преобразований. В этом случае наблюдатели в СО

        имеют дело с состоянием (точнее, с ансамблем состояний),
        приготовленным одним и тем же прибором, и пользуются (одинаковыми)
        измерительными приборами. Последние находятся в различной взаимной
        ориентации (или состоянии движения) и по отношению к создающему
        прибору и потому соответствуют, вообще говоря, разным наблюдаемым.
        Поэтому пассивное преобразование

        описывается отображением множества наблюдаемых (операторов) на
        себя:

        при неизменных векторах состояния

        Ранее было показано, что принцип относительности любому пассивному
        преобразования симметрии сопоставляет некоторое активное, и
        различать их имеет смысл, лишь поскольку обратное верно не всегда.
        Квантовомеханическое описание формально стирает это различие.
        Действительною, любому преобразованию

        можно сопоставить преобразование операторов

        потребовав выполнения равенства

        Это позволяет говорить о преобразовательных свойствах наблюдаемых
        без необходимости перехода к пассивной точке зрения. Особо важную
        роль в квантовой механике играют совокупности наблюдаемых

        для которых соотношение

        задает линейное представление группы симметрии

        Здесь
         ---
        матрица линейного представления группы

        Такие операторы

        называют {(контравариантными) тензорными операторами.
        (Ковариантные тензорные операторы}
         ---
        те для которых преобразование

        эквивалентно контраградиентному к

        преобразованию
        .)

        {Пример}.
        Операторы координат

        или углового момента
         ---
        тензорные операторы относительно группы вращений.

        Обратимся к основному свойству преобразований симметрии в
        квантовой механике. Вероятности переходов

        для исходных состояний и преобразованных

        должны совпадать (противное означало бы несоответствие динамик
        состояний
        ):
        Исключительную важность этого свойства подчеркивает следующая
        теорема.

        {Теорема Вигнера.}
        Пусть преобразование

        удовлетворяет соотношению

        Тогда найдется унитарное или антиунитарное преобразование

        такое, что

        (отсюда следует, что с точностью до постоянной фазы такое
        преобразование

        единственно). Доказательство см. в

        а также в

        Напомним, что преобразование

        называется {антиунитарным}, если

        Соответствующий оператор

        обладает свойством антилинейности:

        {Замечание}.
        Композиция двух антиунитарных преобразований --- преобразование
        унитарное.

        Теорема Вигнера утверждает, что мы не в состоянии отличить
        преобразования симметрии от унитарных (или антиунитарных)
        преобразований в

        и потому можно (и даже следует) отождествлять их с таковыми.

        Посмотрим, к чему это приводит для всей совокупности
        преобразований

        Из соотношения

        следует, что отображение

        в группу

        всех унитарных  преобразований в

        будет воспроизводить закон композиции только на состояниях, т. е.
        лучах

        но не на их представителях

        Таким образом, произведение операторов

        совпадает с

        лишь с точностью до фазового множителя:

        Здесь

        но этого не требуется для следующей, более общей структуры.

        Отображение группы

        в группу обратимых линейных и антилинейных операторов

        в гильбертовом пространстве

        при котором закону композиции в

        соответствует соотношение

        где
         ---
        численный множитель, называется {проективным представлением}
        группы

        Множители

        называются {мультипликаторами} проективного представления.

        Свойства проективных представлений, такие, как приводимость,
        полная приводимость и т. д., формулируются так же, как и в случае
        обычных представлений. (Последние при необходимости подчеркнуть их
        отличие от проективных называют {векторными}.)

        Например, проективное представление называется унитарным, если
        осуществляется унитарными операторами (матрицами). Очевидно, что
        мультипликаторы

        такого представления по модулю равны 1.

        Таким образом, в квантовой механике мы имеем дело с проективным
        представлением группы симметрии. Его почти всегда можно считать
        унитарным, поскольку допускаемая теоремой Вигнера альтернатива
        может относиться лишь к существенно дискретным преобразованиям
        (таким, как

        отражения). Действительно, пусть

        связная подгруппа Ли группы симметрии

        (например, связная компонента единицы в
        ).
        Поскольку любой элемент

        является произведением некоторых

        где

        окрестность единичного элемента, то оператор

        представ{и}м в виде

        где
         ---
        совокупная фаза. Напомним, что окрестность

        всегда может быть выбрана так, что любой ее элементъ

        есть квадрат некоторого элемента

        ей же принадлежащего. Тогда в силу замечания

        только унитарные операторы соответствуют элементам из

        и на основании соотношения

        любому элементу

        Как видим, лишь некоторые элементы дискретной группы симметрии
        могут быть представлены антиунитарными операторами в

        Оказывается, если группа симметрии содержит операцию

        обращения времени

        искомый оператор с необходимостью антиунитарен. Элементарные
        соображения, приводящие к такому выводу, основаны на
        положительности энергии физических состояний, что исключает
        перемену знака оператора энергии

        при перестановке с

        Совпадение динамик исходных и преобразованных состояний (оператор
        развития имеет вид
        )
        при наличии

        отражения

        в этих условиях можно обеспечить, лишь допустив, что `` пронесение
        '' оператора преобразования симметрии через

        сопровождается заменой

        Итак, основными свойствами преобразований симметрии в квантовой
        механике являются их линейность (антилинейность) в пространстве

        векторов состояния и унитарность (антиунитарность) соответствующих
        операторов. Именно поэтому допустимы непосредственная
        теоретико-групповая формулировка и решение задач квантовой
        механики в рамках теории представлений групп. Свойство
        проективности этих представлений обсудим в следующем параграфе.

        Важная особенность квантовомеханической симметрии заключается в
        том, что она позволяет дать простую формулировку такого свойства,
        как элементарность рассматриваемых физических объектов ---
        фундаментальных частиц. Поскольку представление группы симметрии

        оказывается унитарным, то оно полностью приводимо, и

        распадается на неприводимые подпространства

        Неизбежно возникает вопрос о роли (эрмитовых) операторов

        с нетривиальными матричными элементами, связывающими различные
        неприводимые подпространства. Соответствующие наблюдаемые
        осуществляют дополнительную детализацию структуры `` элементарного  ''
        объекта (помимо той, что заложена в группе
        ).
        Возможно, мы недооценили симметрию рассматриваемого объекта (``
        элементарный ''объект с группой симметрии

        конечно, обладает более богатой внутренней структурой). Тогда либо
        получим неприводимое представление более широкой группы симметрии

        либо вновь придется решать проблему происхождения операторов типа

        В последнем случае может оказаться, что по некоторым причинам
        таким операторам не соответствуют никакие наблюдаемые, однако
        отсутствие наблюдаемых типа

        может быть гарантировано лишь в случае неприводимости

        {Замечание}.
        Нам известна лишь одна причина такого запрета --- {принцип
        суперотбора}, формулируемый для таких ({центральных}) наблюдаемых

        измерение которых всегда возможно (т. е. они входят в любой полный
        набор. --- См.
        ).
        Это, например, электрический заряд

        барионный заряд

        и т. д. Принцип суперотбора ограничивает область применимости
        принципа суперпозиции, накладывая запрет на образование линейных
        комбинаций векторов из подпространств

        соответствующих различным собственным значениям

        хотя бы одного центрального оператора

        (Такая комбинация не может описывать физическое состояние,
        поскольку тогда вероятность для наблюдаемой

        иметь определенное значение отличалась бы от 1.) Равным образом
        невозможно существование наблюдаемой, оператор которой имеет
        ненулевые матричные элементы, связывающие подпространства

        он не коммутирует с оператором

        что противоречит определению центральной наблюдаемой.
        Следовательно, по принципу суперотбора нежелательные наблюдаемые
        можно исключить, отнеся

        неприводимые подпространства

        к различным собственным значениям какой-либо центральной
        наблюдаемой. Тогда объект, описываемый прямой суммой двух и более
        таких

        заведомо неэлементарен.

        На основании изложенного объект (частицу) следует считать
        {элементарным}, если пространство его векторов состояния

        неприводимо относительно группы

        физической симметрии.

        {Замечание}.
        В таком случае элементарными, строго говоря, можем считать только
        стабильные частицы. Действительно, как будет показано в следующей
        главе, неприводимые унитарные представления группы Пуанкаре
        характеризуются фиксированным значением массы (собственным
        значением оператора Казимира этой группы
        ).
        Нестабильным же частицам в силу теоремы Фока-Крылова (см.,
        например,
        )
        должно соответствовать непрерывное распределение по энергии и,
        следовательно, по массе. Равноправное рассмотрение стабильных и
        распадающихся частиц как элементарных может быть оправдано
        следующим. Объединение тех и других производится при обнаружении
        признаков более высокой симметрии (скажем, изотопической,
        унитарной и т. д.), которую можно рассматривать лишь как
        приближенную (нарушенную). Нестабильность части состояний из ее
        мультиплета --- (неприводимого) представления соответствующей
        группы --- обусловлена той же причиной, что и нарушение симметрии.
        Можно думать, что если бы (в каком-либо гипотетическом мире)
        симметрия не нарушалась, то все состояния из данного мультиплета
        были бы стабильны.

        Формализм квантовой механики отражает единство понятий симметрии и
        наблюдаемых. Действительно, наблюдаемым сопоставлены эрмитовы
        операторы в

        Преобразования симметрии реализуются тоже операторами в
         ---
        унитарными. Тогда генераторы

        группы симметрии

        представлены антиэрмитовыми операторами

        Эрмитовы операторы

        естественным образом определяют набор (основных) наблюдаемых.
        Алгебра наблюдаемых

        совпадает с алгеброй Ли

        группы

        в базисе, упомянутом в замечании

        (Здесь предполагается, что представление

        векторное. Обоснование этому будет дано в следующем параграфе.)

        Наблюдаемым, зависящим от

        т. е. (функциям), соответствуют элементы обертывающей алгебры

        Разумно предположить, что алгебра наблюдаемых не содержит иных, т.
        е. не связанных с какой бы то ни было физической симметрией.

        В рамках квантовомеханического описания оказывается возможным
        сопоставить наблюдаемые существенно дискретным симметриям (что
        лишено смысла в классической теории).

        Рассмотрим, например,

        отражения. (Строго говоря, соответствующая симметрия в природе
        отсутствует.)

        Отражение

        трех пространственных осей представимо комбинацией отражения
        относительно одной оси и вращения.
        Следовательно, при наличии

        симметрии своийства операции

        определяются свойствами зеркального отражения. Экспериментальная
        проверка такой симметрии заключается в установлении совпадения
        динамики

        точной (до последнего существенного `` винтика '') зеркальной
        копии

        рассматриваемого объекта

        поставленного в соответствующие
        (``
        отраженные '') внешние условия, с наблюдаемой в зеркале динамикой
        исходного.

        Для макроскопических объектов

        симметрия имеет место и проявляется в инвариантности уравнений
        движения классической механики относительно преобразований

        Не исключено, что в микромире найдутся объекты, не имеющие ``
        зеркального '' двойника.

        Возможно, что такими обьектами являются нейтрино, например
        электронное
        ---
        частица спина 1/2 с практически нулевой массой
        .
        Точнее, при

        следует говорить не о спине, а о спиральности
        (см. ()).
        Спиральность

        есть проекция спина частицы на направление ее движения. Спин
        частицы
        ,
        орбитальный момент
        ,
        полный угловой момент

        и спиральность

        при операции

        преобразуются так:

        Следовательно, для самой постановки вопроса о
        -
        симметрии необходимо существование нейтрино с

        (правых) и

        (левых). Только тогда будет иметь смысл операция
        -
        отражения в пространстве состояний. Четких экспериментальных
        свидетельств существования правых нейтрино до сих пор не получено.
        В любом случае поведение других частиц, скажем, правых и левых
        электронов, в процессах слабого взаимодействия несимметрично.
        Напротив, сильное и электромагнитное взаимодействия демонстрируют
        симметрия относительно
        -
        отражения.

        Поскольку двукратное отражение приводит к исходному состоянию и
        оператор

        унитарен, то

        Строгое рассмотрение проективности представления операции

        будет проведено в гл. 9, а здесь для простоты положим

        Тогда векторы состояний

        --- собственные для оператора
        .
        Его собственные значения +1 и -1 называются
        -
        {четностью} (или просто {четностью}) состояний
        .
        Это (мультипликативное) квантовое число сохраняется в процессах
        сильного и электромагнитного взаимодействий. Его можно приписать
        всем состояниям участвующих в таких процессах частиц. (В
        действительности вследствие проективности в случае частиц
        полуцелого спина речь может идти лишь об относительной четности.)

        Аналогично в квантовой физике возникают наблюдаемые,
        соответствующие другим дискретным симметриям, например зарядовой

        для состояния частиц и античастиц.

        {Изотопическая симметрия и операторные лучи. Мультипликаторы и
        коциклы проективного представления. Фазовые расширения.
        Эквивалентность проективных представлений группы и вектроных
        представлений ее универсальной накрывающей}

        Уже установлено, что представление группы симметрии в пространстве
        векторов состояния

        является проективным. Таким образом, возникает задача
        классификации неэквивалентных (неприводимых, унитарных)
        проективных представлений заданной группы симметрии.

        Уточним это на примере изотопической симметрии адронов
        (сильновзаимодействующих частиц). Ядра атомов можно считать
        состоящими из частиц двух сортов --- протонов

        и нейтронов
        ,
        связанных ядерными силами (сильным взаимодействием), не зависящими
        от сорта частиц. Различие во взаимодействии, скажем, протона с
        протоном ---
        ,
        протона с нейтроном ---
        ,
        вполне объяснимо действием электромагнитных сил (вследствие
        различия электрического заряда

        у протона
        ()
        и нейтрона
        ()).
        Сужение матрицы оператора заряда

        на прямую сумму

        подпространств состояний протона

        и нейтрона
        ,
        таким образом, имеет вид

        Оператор барионного заряда

        (здесь он совпадает с нуклонным зарядом) представится в таком
        случае единичной матрицей. При этом состояния протона и нейтрона
        (как собственные состояния оператора

        они ортогональны) имеют вид

        (Нас пока не интересуют другие квантовые числа и структура
        волновых функций для
        и .)
        Отметим, что вследствие суперотбора по

        векторам

        не соответствуют реальные состояния. Однако этот суперотбор
        является практически единственной причиной, накладывающей запрет
        на такие комбинации (если не считать нестабильности нейтрона,
        вызванной слабым взаимодействием). Сильные взаимодействия
        нечувствительны к величине электрического заряда. Можно думать,
        что в воображаемом мире, где `` выключены '' слабые и
        электромагнитные силы, комбинации вида

        равноправны с векторами
        ,
        и подстановки

        не сказываются на структуре ядерной материи. Этим комбинациям
        соответствуют отличные от 1 и 0 средние значения электрического
        заряда, что, повторяем, с точки зрения ядерных сил несущественно.
        Существенным следует считать лишь необходимость рассматривать две
        такие комбинации: (внутреннее) пространство состояний нуклонов
        двумерно
        (см.()).
        Оно называется {изотопическим}.

        Итак, ограничиваясь рассмотрением ядерных сил, преобразование

        можно считать преобразованием физической ({изотопической})
        симметрии. При этом должны сохраняться вероятности переходов.
        Амплитуды этих вероятностей означают нормиоровку
        и
        и их ортогональность. Следовательно,

        матрица

        преобразования

        в

        должна быть унитарной.

        Эти операторы

        осуществляют проективное представление группы

        изотопической симметрии:

        Установить, что это за группа, в данном случае сравнительно
        несложно. Следует исходить из того, что представление

        является определяющим и потому точным.

        На суперпозиции

        нет иных ограничений кроме унитарности: все они, как уже
        подчеркивалось, равноправны. Однако множество

        содержит матрицы
        ,
        кратные единичной. Они описывают тривиальные фазовые
        преобразования вектров
        ,
        не отражающиеся на физических состояниях. Для векторного
        представления группы

        сразу последовал бы вывод:
        .

        Убедимся, что в данном случае
        .

        По аналогии с состояниями --- лучами

        в
        ---
        введем {операторные} лучи
        ---
        классы операторов вида
        .
        Естественный закон композиции

        наделяет множество лучей

        структурой группы и очевидно точно воспроизводит закон композиции
        в
        :

        Вместе с тем очевидно, что операторные лучи совпадают с классами
        смежности

        и произведение

        является произведением в группе
        .

        В рассмотренном примере

        заложена схема действий, необходимых для установления структуры
        группы той или иной симметрии. Предположим, например, что каждый
        кварк (из которых по современным воззрениям `` составлены ''
        адроны) может пребывать в трех независимых (`` цветных '')
        состояниях, к различию которых нечувствительны межкварковые силы.
        Рассуждая аналогично предыдущему, читатель убедится, что группа
        цветной симметрии изоморфна

        (она обозначается через
        ).

        Вернемся к исследованию проективных представлений. На примере

        мы фактически установили, что группа операторных лучей
        ,
        однозначно определяемая точным проективным представлением
        ,
        изоморфна группе симметрии
        .
        Очевидно, что одной и той же группе

        соответствуют разные представления
        в ---
        согласно выбору представителей в классах
        .
        Столь же очевидно, что все они физически эквивалентны и в данной
        задаче классификации проективных представлений не имеет смысла
        различать их.

        Совокупность лучей

        не образует линейного пространства, поэтому действующие на них
        операторные лучи

        не образуют векторного представления группы
        .
        Однако если бы

        всегда восстанавливалась по содержащемуся в ней векторному
        представлению

        некоторой группы

        (необязательно совпадающей с
        ), то наша задача

        полностью свелась бы к классификации векторных представлений
        групп.

        Итак, {эквивалентными} мы считаем представления
        и ,
        связанные соотношением
        ,
        нде фаза
        ---
        произвольная функция. Можно доказать
        (см., например,
        ),
        что класс

        эквивалентных представлений должен содержать хотя бы одно такое, в
        котором операторнозначная функция
        на
        сильно непрерывна в подходящей окрестности
        .
        Доказательство основывается на физически естественном
        предположении, что средние значения наблюдаемых в состояниях

        непрерывно зависят от
        .

        В этом случае мультипликаторы

        такого представления должны быть непрерывными по обоим аргументам.
        Мултипликаторы
        и
        эквивалентных представлений назовем {эквивалентными}.

        Ассоциативность группового умножения накладывает на функции

        ограничение

        которое следует из сравнения двух способов расстановки скобок в
        произведении
        .

        Ограничение

        (см. ())
        удобно удовлетворить тождественно, положив
        .
        Функцию

        (она непрерывна, если такова
        )
        будем называть {коциклом} данного проективного представления.
        Требование ассоциативности применительно к

        имеет вид

        Из соотношения

        следует, что между эквивалентными мультипликаторами существует
        связь
        ,
        автоматически согласованная с условием
        .
        Тогда для эквивалентных коциклов получает
        ,
        где
        .

        Коцикл

        удовлетворяет требованию ассоциативности

        тогда и только тогда, когда ему удовлетворяет
        .

        Для исключения тривиальной эквивалентности, соответствующей
        постоянной фазе

        потребуем, чтобы
        .
        Это ограничивает фазы преобразований эквивалентности условием
        .
        Всюду в дальнейшем требования

        будут предполагаться выполненными.

        При исследовании класса эквивалентных коциклов основная идея
        заключается в изучении локальных свойств коциклов
        ,
        а затем в рассмотрении того, в какой мере их можно распространить
        на группу

        в целом. Остановимся на узловатых моментах (несложные
        доказательства читатель найдет в оригинальной работе
        ).

        Класс эквивалентности любого коцикла
        ,
        как уже известно, содержит хотя бы один непрерывный.

        {Теорема.}
        Для любого коцикла

        группы Ли

        его класс эквивалентности содержит дифференцируемый локалный
        коцикл.\{Если рассматриваемое свойство коцикла определено или
        существенно только в окрестности единичного элемента группы,
        коцикл будем называть {локальным}. {Дифференцируемый коцикл} ---
        дифференцируемая функция канонических координат группы.}

        Можно установить, что в классах эквивалентности всегда содержатся
        коциклы специального вида. Коцикл

        будем называть {каноническим}, если:

        дифференцируемый коцикл;

        для элементов

        из одной и той же однопараметрической подгруппы.

        {Лемма.}
        Любой локальный коцикл

        группы Ли

        эквивалентен каноническому локальному коциклу.

        Сужение отношения эквивалентности на канонические коциклы
        приводит к важному результату.

        {Лемма.}
        Если локальный коцикл

        канонический и коцикл

        эквивалентен ему, где
        ---
        фаза преобразования, то коцикл

        является локальным каноническим тогда и только тогда, когда
        ---
        линейная форма канонических координат группы

        (в соответствующей окрестности единицы).

        Для установления аналитичности локального канонического коцикла
        конструируется вспомогательная группа. (Она же помогает выяснить,
        в какой мере глобальный коцикл восстанавливается по локальным
        свойствам.)

        Происхождение этой вспомогательной группы можно пояснить,
        рассмотрев умножение операторных лучей
        .
        Проективное представление

        фиксирует представителей в классах
        .
        Произведение произвольных операторов

        имеет вид
        .
        Множество операторов

        образует представление группы

        с композицией
        .
        Справедливость групповых аксиом

        для умножения

        вытекает из свойств коцикла
        :
        соотношения

        и нормировки
        .

        Назовем группу

        {фазовым расширеним} группы
        .
        Множество

        элементов вида

        принадлежит ее центру, и факторгруппа по нему изоморфна исходной
        группе
        ,
        т. е. фазовое расширение

        является центральным расширением группы

        (см. ()).

        Очевидно, что эквивалентные коциклы
        и
        определяют изоморфные фазовые расширения
        и ---
        изоморфизм выражается соотношением
        .

        Удобно не различать эти группы, т. е. фазовое расширение

        поставить в соответствие свему классу эквивалентных коциклов,
        интерпретируя соотношение

        как репараметризацию
        .

        Зафиксируем в

        параметризацию элементов
        :
        взяв в качестве

        канонические координаты группы
        ,
        а в качестве
        ---
        параметр

        при условии, что коцикл

        в
        ---
        локальный кононический. Тогда
        ---
        локальная группа Ли, и канонические локальные коциклы в классе

        определяют (локальные) канонические координаты на
        .
        (Проверка этого утверждения элементарна.) Как известно, в
        канонических координатах группы Ли функции
        ,
        и в частности
        , ---
        аналитические, т. е. справедлива следующая теорема.

        {Теорема.}
        Локальный канонический коцикл
        ---
        аналитическая функция локальных канонических координат группы
        .

        Структурные константы

        локальной группы

        имеют вид

        где

        структурные константы исходной группы
        .

        Матрица

        определяет антисимметричную билинейную форму

        на алгебре Ли

        группы

        Форму

        назовем {инфинитезимальным коциклом}.

        Определим по ней (трилинейную) форму

        Тогда основное свойство

        кратко записывается в виде

        Действительно, последнее эквивалентно требованию

        которое есть тождество Якоби для структурных констант

        алгебры Ли

        группы

        Итак, каждому (локальному) каноническому коциклу

        проективного представления группы

        соответствует инфинитезимальный коцикл

        билинейная антисимметричная функция на алгебре

        удовлетворяющая соотношению

        Верно и обратное утверждение (доказательство сводится к задаче
        восстановления локальной группы Ли

        по ее алгебре Ли).

        Отношение эквивалентности коциклов

        индуцирует эквивалентность инфинитезимальных коциклов

        где

        некотороя билинейная антисимметричная форма на

        Напомним
        (см. лемму ()),
        что фаза преобразования, связывающего канонические коциклы

        есть линейная форма канонических координат (в
        ).

        Определим линейную форму

        на

        В соответствии с

        матрицу

        формы

        можно получить дифференцированием выражения

        откуда

        (см. формулу Кэмпьелла-Хаусдорфа
        ).
        `` Выживает '' только вклад коммутатора

        т. е.

        Итак, исследование классов эквивалентных проективных представлений
        сведено к построению классов эквивалентности инфинитезимальных
        коциклов
        .
        Множество таких
        ---
        линейное пространство, размерность

        которого не может превышать числа
        ,
        поскольку матрицы

        антисимметричны и подчинены системе линейных соотношений
        .
        (Она тривиальна лишь в случае абелевой группы
        ,
        т. е. в остальных случаях
        .)
        Размерность

        линейного пространства классов

        эквивалентных инфинитезимальных коциклов

        где
        ---
        размерность подпространства коциклов вида
        .
        (Для абелевой группы, как это следует из

        т. е.
        .)

        {Упражнение}.
        Покажите, что коциклы

        определяют изоморфные фазовые расширения.

        Задача построения классов эквивалентности

        допускает решение для каждой конкретной группы

        Она фактически решена нами для группы вращений

        Галилея и Пуанкаре. Действительно, в проективном представлении

        коммутационыые соотношения для генераторов группы

        воспроизводятся в виде

        К такому же виду скобочных структурных соотношений мы пришли в

        (см. формулу
        ).
        (Отметим, что принцип соответствия скобке Пуассона

        сопоставляет
        .)
        Сдвигу преобразующих функций (генераторов) на постоянную точно
        соответствует преобразование эквивалентности

        порождающее сдлиг

        Результат проведенного в

        исследования устранимости констант в структурных соотношениях
        можно переформулировать следующим образом. В проективном
        представлении алгебры Ли

        группы Галилея возможна только одна неустранимая константа,
        следовательно, пространство

        одномерно:

        Для группы вращений

        и группы Пуанкаре, у которых все константы устранимы,

        Все инфинитезимальные коциклы этих групп эквивалентны нулевому.

        {Замечание}.
        В калибровочных теориях элементарных частиц наиболее
        привлекательными физическими чертами обладают модели с
        полупростой группой симметрии. Для таких групп справедлива
        следующая теорема.

        {Теорема.}
        Любой инфинитезимальный коцикл полупростой группы Ли эквивалентен
        нулю.

        Для доказательства используется невырожденность матрицы

        формы Киллинга полупростой группы

        Тогда в левой части

        условия

        можно обратить `` коэффициент '' при

        в каком-либо слагаемом и переписать это условие:

        Следовательно,

        имеет вид

        Итак, задачу нахождения классов эквивалентных инфинитезимальных
        коциклов группы симметрии будем считать решенной. Предположим, что
        все коциклы эквивалентны нулевому. Это означает, что фазовое
        расширение

        локально изоморфно прямому произведению

        Тогда всякое проективное представление группы

        эквивалентно векторному представлению ее локальной группы (и
        алгебры Ли). Как известно, эти представления совпадают с
        векторными представлениями исходной группы

        только для односвязных групп.

        {Утверждение}.
        Проективное представление связной группы

        при

        эквивалентно векторному представлению ее универсальной
        накрывающей

        До облуждения случая

        установим, что при определенных условиях задача

        не требует обращения к свойствам

        {Теорема.}
        Конечномерное непрерывное проективное представление

        связной односвязной группы Ли

        эквивалентно векторному ее представлению.

        {Доказательство}.
        Пусть
        ---
        мультипликатор представления

        (не предполагаемого унитарным):

        (---
        непрерывная функция, поскльку представление

        непрерывно). Определители конечномерных матриц

        удовлетворяют соотношению

        Определим матрицы

        положив

        Отображение

        в подходящей окрестности

        можно сделать непрерывным, выбрав фиксированную ветвь корня

        степени. Отображение

        однозначно и непрерывно продолжается на всю группу

        в силу ее односвязности. Мультипликатор

        представления

        непрерывен. Так как он удовлетворяет соотношению

        то является константой:

        Тогда мультипликатор

        представления

        равен единице.

        Применение подобной процедуры к случаю группы вращенй

        подчеркивает роль односвязности. Действительно, в

        соотношения

        определяют представление с мультипликатором

        равным единице. Однако невозможно гарантировать совпадения
        непрерывного продолжения функции

        в точку

        вдоль двух негомотопных путей. В таком случае придется отказаться
        либо от непрерывности, либо от однозначности представления

        {Замечание}.
        В случае группы вращений

        среди неприводимых представлений ее универсальной накрывающей

        содержатся (при полуцелом спине) двузначные, не являющиеся
        векторными представлениями в обычном смысле. Их можно
        интерпретировать как проективные представления

        с дискретным мультипликатором

        Как видим, возможность существования объектов (элементарных
        частиц), описываемых такими представлениями, вытекает из самых
        общих принципов квантовой механики и не имеет аналога в
        классической физике.

        Обсудим общий случай

        Проективное представление

        определяет векторное представление

        фазового расширения

        (по построению). Определив классы

        нетривиальных инфинитезимальных коциклов, построим соответствующие
        расширения

        и исследуем их векторные (неприводимые унитарные) представления.
        (Как и прежде, речь должна идти о представлениях односвязной
        накрывающей

        которая также представляет собой расширение, но уже по дискретной
        подгруппе.) Из последних отберем лишь такие, для которых сужение
        на центральную подгруппу

        состоит из скалярных операторов

        (см.
        --- построение операторов
        ).
        Таким образом, квантовомеханическая проблема классификации
        (неприводимых, унитарных) проективных представлений группы
        симметрии сводится к стандартной задаче теории линейных
        предствленний для накрывающей

        Поэтому группу

        принято называть группой квантовомеханической симметрии. В
        частности,

        называется {квантовомеханической группой Пуанкаре};
        ---
        {квантовомеханической группой Лоренца}.

        Для абелевой группы размерности

        Однако в частном случае, который потребуется в дальнейшем,
        справедливо следующее утверждение.
        {Теорема Баргмана.}
        Проективное представление компактной связной абелевой группы

        эквивалентно ее векторному представлению. (Доказательство см. в
        .)
        Другим примером группы с

        является группа Галилея

        В силу соотношения

        (см.
        )
        дополнительному генератору расширения
        следует придать смысл оператора массы

        {Замечание}.
        Этим устраняется неравноправие наблюдаемой

        в нерелятивистском случае: массе

        до сих пор мы не могли сопоставить ни генератор группы симметрии,
        ни элемент обертывающей алгебры.

        Различные значения

        определяют изоморфные расширения

        (см. упражнение
        ),
        так что достаточно исследовать представления

        при какой-либо фиксированной массе.

        В случае симметрии Галилея есть веские основания ограничиться
        только указанными представлениями, т. е. не рассматривать класс
        эквивалентности нулевого коцикла и соответствующих векторных
        представлений группы

        Действительно, в неприводимом прдеставлении группы Галилея
        оператор Казимира

        является числом, поэтому волновые функции

        в импульсном представлении заданы на сфере

        В силу канонических квантовомеханических коммутационных
        соотношений

        переход к координатному представлению

        осуществляется преобразованием Фурье. Как известно, при наличии
        ограничения

        функция

        не может быть локализована.

        Такая трудность не возникает в проективном представлении группы

        с нетривиальным коциклом

        Действительно, в силу соотношения

        любое ограничение типа

        оказывается невозможным,

        может принимать любое значение из

        может быть локализована. Таким образом, симметрия Галилея (по
        необходимости) является примером нетривиального отличия
        квантовомеханической группы симметрии от соответствующей
        классической.

        {Изотопические мультиплеты, формула Гелл-Манна-Нишиджимы.
        Зарядовое сопряжение и

        четность}

        В случае изотопической

        симметрии результаты предыдущего параграфа означают, что
        пространство векторов состояния сильновзаимодействующих частиц
        (адронов) является пространством векторного представления группы

        Генераторы

        этой группы

        суть наблюдаемые --- компоненты изоспина. В полный набор может
        войти лишь их линейная комбинация, коммутирующая с электрическим
        зарядом

        Пусть это будет

        Двумерное (внутреннее) подпространство состояний нуклонов
        реализует спинорное представление группы

        Здесь операторы

        имеют вид

        где

        матрицы Паули.

        Отметим, что в

        операторы наблюдаемых
        и
        оказываются связанными соотношением

        где единичную

        матрицу

        можно рассматривать как сужение на

        некоторого оператора, называемого оператором {гиперзаряда}

        {Эмпирическое соотношение}

        называется {формулой Гелл-Манна-Нишиджимы}.

        В

        действует фундаментальное представление

        Следующее представление

        имеет размерность

        Оно вещественно (см.
        ),
        а векторы состояний должны быть собственными для

        т. е., вообще говоря, комплексными.

        Для определенности рассмотрим триплет

        мезонов --- псевдоскалярных частиц (скалярность означает, что спин

        а приставка `` псевдо '' --- внутреннюю четность
        ).
        Для пионов

        Пусть

        базисные векторы неприводимого представления группы

        Пространство

        наряду с каждым вектором

        содержит луч

        а следовательно, и нормированные собственные векторы оператора

        соответствующие собственным значениям
        и
        Именно эти векторы описывают реальные состояния. Их совокупность
        называется изотопическим мультиплетом

        мезонов. Напомним, что вследствие нарушенного характера
        изотопической симметрии они разделены в силу суперотбора по

        В приближении симметрии, что эквивалентно пренебрежению правилом
        этого суперотбора, связь векторов мультиплета

        с векторами

        имеет смысл и взаимно-однозначна.

        В общем случае

        симметрии ее мультиплеты состоят из частиц, векторы состояний
        которых --- собственные для ее генераторов, допускающих включение
        в полный набор наблюдаемых. Без ограничения общности в качестве
        этих генераторов можно взять элементы базиса подалгебры Картана
        группы

        Тогда ее весовые диаграммы адекватно отражают содержание
        мультиплетов.

        Изотриплет пионов удобно представлять в виде эрмитовой

        марицы

        где

        волновая функция для вектора

        Множитель

        здесь введен для нормировки на единицу волновых функций
        независимых состояний

        относительно скалярного произведения

        Преобразование

        реализуется операцией

        Выразив матричные элементы

        через волновые функции состояний мультиплета

        получим

        Представлением вида

        является также изотриплет

        {гиперонов}

        --- частиц с такими же спином и четностью, как и нуклоны:

        (что кратко обозначается как
        ).

        Для этого триплета гиперзаряд

        также равен нулю и заряд

        Однако в отличие от матрицы волновых функций пионов

        выражение

        неэрмитово. Эрмитово сопряженной

        следует считать матрицу волновых функций античастиц

        В данном случае имеем дело с операцией

        зарядового сопряжения. Она сопоставляет состояниям частицы

        состояния соответствующей античастицы

        с комплексно сопряженными волновыми функциями (с точностью до
        фазового множителя
        который для простоты будем считать равным
        ).

        В рамках релятивистской квантовой теории поля необходимость
        введения античастиц связана с требованием полноты системы
        состояний (массивных) частиц с положительной энергией. Вообще
        говоря, допустимо совпадение состояния частицы

        и античастицы

        Такие частицы называются {истинно нейтральными}. (Например,

        {мезон}.) {Нейтральным} называется также мультиплет, если для
        каждой входящей в него частицы он содержит и ее античастицу.
        (Изотриплет пионов обладает данным свойством.)

        Убедимся, что нейтральной частице (мултиплету) соответствуют
        нулевые значения аддитивных центральных наблюдаемых. (Для
        нейтрального мультиплета --- только тех, что коммутируют с группой
        симметрии. Например, не все состояния в изотриплете

        соответствуют нулевому электрическому заряду

        на, как отмечалось, именно эта наблюдаемая теряет право на
        суперотбор в приближении изотопической симметрии.)

        Напомним, что по определению центральные наблюдаемые

        коммутируют друг с другом и со всеми остальными наблюдаемыми. Их
        аддитивность позволяет предположить, что наблюдаемые

        суть генераторы некоторой абелевой группы симметрии

        Подчеркнем, что

        должна быть компактна, поскольку спектр зарядов

        дискретен. Достаточно рассмотреть только ее связную компоненту
        единицы

        с существенно дискретными элементами мы связываем
        {мультипликативные} квантовые числа.

        Итак,

        связная компактная абелева группа. Пусть

        канонические координаты на ней. В силу теоремы

        ее квантовомеханическое представление эквивалентно векторному. На
        подпространствах состояний элементарных частиц оно унитарно и
        неприводимо, а потому одномерно:

        Здесь `` заряды ''

        значения наблюдаемых

        характеризующие данную частицу

        Тогда матрица волновых функций, эрмитово сопряженная, скажем, к
        матрице пионов

        (или

        гиперонов
        ),
        описывает состояния мультиплета, являющегося представлением группы

        комплексно сопряженным

        Таким образом, античастица

        характеризуется противоположными по знаку зарядами

        и ее состояния могут совпасть с состояниями частицы лишь в случае
        нулевых значений всех

        Последнее выполняется для триплета пионов, но не для

        гиперонов, у которых нетривиально значение барионного заряда

        О

        симметрии следует сказать примерно то же, что и в случае

        отражений (см.
        ).

        Обозначим вектор состояния, например нейтрино (антинейтрино) со
        спиральностью

        через

        Динамика процессов слабого взаимодействия не позволяет говорить о
        симметрии относительно операций:

        {Замечание}.
        `` Комбинированное '' преобразование

        можно считать преобразованием симметрии в слабых взаимодействиях.
        Хотя динамики состояний

        и не коммутируют с операцией

        соответствующие процессы еще менее интенсивны, чем процессы
        слабого взаимодействия (вызывающее их взаимодействие названо
        сверхслабым). Отметим, что в силу СРТ теоремы (см., например,
        )
        нарушение

        симметрии эквивалентно нарушению

        симметрии.

        Как и в случае

        отражений, зарядовая симметрия приводит к (мультипликативному)
        квантовому числу ---

        {четности}, сохраняющемуся в электоромагнитных и сильных
        взаимодействиях. Согласно правилу суперотбора аналогичные

        собственные состояния существуют лишь для истинно нейтральных
        частиц, и только они характеризуются опредленной

        четностью.

        Приписывание определенной

        четности нейтральному мультиплету (например, изотриплету
        ),
        вообще говоря, несостоятельно.

        Это станет очевидным, если на примере пионов рассмотреть действие
        операции

        на состояниях

        Воспользовавшись соотношенями

        можно получить

        т. е. по существу

        сопряжение есть зеркальное отражение второй оси в трехмерном
        изотопическом пространтсве

        представления

        Очевидно, что такая операция не может коммутировать с группой

        Указанный недостаток можно устранить, дополнив

        сопряжение поворотом вокруг второй оси до полного отражения
        в
        Наблюдаемая, соответствующая этой операции, называется

        четностью и является важной характеристикой нейтральных
        изомультиплетов с целым (и только целым!) значением изоспина. В
        общепринятой краткой нотации указывается только знак

        четности частицы, ровно как и знаки

        четности и

        четности

        его нейтральной компоненты:

        Например, для пионов записываем


        {Унитарная симметрия и унитарные мультиплеты. Эволюция унитарной
        симметрии}

        Существование нескольких совокупностей изомультиплетов с примерно
        одинаковыми пространственными свойствами (одинаковыми
        спин-четностями

        и близкими по величине массами) наводит на мысль, что реальная
        симметрия сильных взаимодействий шире, чем

        Изомультиплеты, скажем, со спин-четностью

        кроме нуклонов
        и
        гиперонов, --- это

        гипероны

        и изосинглет

        известные еще в начале 60-х годов, различаются значениями
        гиперзаряда
        1, 0, -1 и 0 соответственно (барионный заряд

        равен единице для всех названных частиц).

        Практически идентичные свойства этих частиц в процессах сильного
        взаимодействия позволили предположить, что истинная симметрия

        последнего шире

        Неприводимое унитарное представление ее группы, содержащее
        изомультиплеты

        конечномерно, следовательно,

        компактна. Ранг минимальной такой группы не меньше двух, поскольку
        в полный набор наблюдаемых кроме

        должно войти не меньше одной независимой, скажем, гиперзаряд

        Группа

        служит для классификации частиц, однако рассматриваемое
        представление относительно нее по-прежнему приводимо. Оставалось
        выбрать

        из четырех простых (полупростых) компактных групп ранга 2. Это
        группы
        и
        Все группы, кроме первой, не подошли либо из-за несоответствия
        содержания изомультиплетов в их неприводимых представлениях
        реальным состояниям, либо из-за нефизических (не наблюдаемых в
        действительности) запретов. (Более подробно с этими вопросами
        можно ознакомиться в
        .)

        Указанные барионы
        и
        погружаются в восьмимерное представление группы

        (симметрия получила название `` унитарной ''). Аналогичная

        матрица

        нормируется по форме

        во внутреннем пространстве. (Здесь не рассматривается проблема
        согласованного выбора фаз векторов для каждого состояния
        мультиплета: унитарной симметрией он не фиксируется и определяется
        удобством реализации операции типа зарядового сопряжения
        .)

        Преобразования симметрии имеют вид

        Изотопической подгруппе соответствуют унитарные

        матрицы

        Аналогичным образом заполняется октет

        псевдоскалярных

        и октет

        векторных

        мезонов

        Как уже было показано в

        содержание мультиплета группы симметрии

        можно описывать весовой диаграммой ее представления. Обозначив
        веса символами соответствующих состояний, например, октетам

        сопоставляем диаграммы

        Отметим десятимерное представление

        (декуплет) с

        Напомним, что классификация

        резонансов

        позволила предсказать существование частицы
        и
        В силу эквидестантности масс
        и
        ожидаемое значение массы этой частицы
        1680 МэВ.
        Обнаружение

        гиперона с требуемыми свойствами явилось триумфом унитарной
        симметрии.

        Дальнейшее развитие

        симметрии происходило в нескольких направлениях. Укажем следующие.

        Интенсивно разрабатывалась теория калибровочных полей (или полей
        Янга-Миллса), основанная на {калибровочной} (или {локальной})
        симметрии (см., например,
        ).
        Последняя отличается от ортодоксальной

        симметрии относительно преобразований

        тем, что ее преобразования на состояниях существенно зависят от
        координат

        характеризующих эти состояния, т. е. преобразования волновых
        функций в координатном представлении

        где

        при всех

        С формальной точки зрения группа калибровочной симметрии

        есть прямое произведение по точкам пространства Минковского:

        причем все

        изоморфны

        (Обычную

        симметрию при этом называют глобальной.)

        Второе направление было обусловлено тем, что совокупность
        известных адронных состояний заполняет далеко не все представления

        низшей размерности. В частности, не обнаружены кандидаты для
        представлений

        а так же для фундаментальных представлений
        и
        Это находит объяснение в кварковой модели. В рамках последней
        унитарная симметрия мыслится как симметрия в мире адронов

        составленных из (`` унитарного '') триплета кварков

        {Ставшие традиционными обозначения первых двух кварков
        соответствуют знаку третьей компоненты изоспина:

        `` up '',

        `` down ''.

        кварк обладает нетривиальной странностью

        `` strange ''. Для отличия от цветных степеней свободы

        типы состояний

        названы `` ароматами '' (`` flavour ''), а группа унитарной
        симметрии --- группой ароматов
        .}

        каждый из которых в свою очередь может находиться
        в одном из трех (`` цветных '') состояний. Этому соответствует
        симметрия нового уровня --- цветная

        (см.
        ).
        (Калибровочная теория с группой

        называется {хромодинамикой}.)

        Прогресс техники эксперимента также отразился на статусе группы

        ее следует считать лишь подгруппой в более широкой группе
        ароматов. Были обнаружены мезонные состояния (в частности,

        мезон), с которыми следует связать новые наблюдаемые (не
        содержащиеся в алгебре Картана группы
        ),
        т. е. группа ароматов

        имеет ранг

        На языке кварков это означает, что кроме

        должны существовать и кварки, соответствующие нетривиальным
        значениям новых наблюдаемых
        `` charm ''
        `` beauty ''
        `` truth ''). Кварки, существование которых мы сейчас имеем
        основания предпологать, см. в табл.

        Экспериментальные данные хорошо согласуются с предположением, что
        группа симметрии ароматов есть

        Перечисленные в таблице кварки принадлежат ее фундаментальному
        мультиплету.

        Интенсивная разработка моделей слабого взаимодействия элементарных
        частиц привела к построению единой теории слабого и
        электоромагнитного взаимодействия (модели Вайнберга-Салама
        )
        и возродила надежды на построение единой теории вообще всех
        взаимодействий.

        Как уже отмечалось, процессы слабого взаимодействия не обладают

        симметрией. Это приводит к тому, что состояния (или их волновые
        функции) с одним значением спиральности и противоположным (если
        таковое существует) входят в теорию несимметрично. (Для краткости
        принято использовать индексы `` L '' (`` левый '') или `` R ''
        (`` правый '') в зависимости от знака спиральности.) Скажем,

        волновая функция `` левого '' позитрона с

        Объединенная теория слабого и электромагнитного взаимодействия ---
        это калибровочная теория, основанная на глобальной группе
        симметрии

        Лептоны

        образуют ее приводимое предствление. Оно приводимо и для каждого
        `` поколения '' --- указанных пар лептонов. Например, левые
        частицы

        являются дублетом, а правый электрон

        синглетом. Преобразовательные свойства остальных поколений точно
        такие же. Если генератор подгруппы

        обозначаемый через

        считать слабым гиперзарядом, то для лептонов существует аналог
        формулы Гелл-Манна-Нишиджимы

        Тогда дублеты

        имеют

        а синглеты (скажем,
        )
        (Для античастиц знаки следует заменить на притивоположные.)

        Необходимым атрибутом калибровочной

        теории являются векторные частицы (кванты калибровочного поля),
        реализующие присоединенное представление (глобальной) группы

        Они служат `` переносчиками '' взаимодействия. В рассматриваемом
        случае их должно быть четыре:

        триплет

        и синглет
        (
        индекс Лоренц-вектора). Поскольку

        входит в

        прямым сомножителем, то эти частицы обладают нулевым слабым
        гиперзарядом

        и спектр

        совпадает со спектром

        Включение в данную теорию адронов подразумевает установление их
        классификации оносительно группы

        В общепринятой модели

        (где оказалось необходимым предположить существование четвертого
        кварка ---
        )
        электромагнитное и слабое взаимодействие адронов есть
        взаимодействие составляющих их кварков с теми же калибровочными
        частицами A и B, с которыми взаимодействуют и лептоны. При этом
        кварки
        и
        сгруппированы в два дублета и четыре синглета (относительно
        ).
        Синглетами являются правые состояния этих кварков, а дублеты

        образованы левыми:

        Здесь

        угол Кабиббо --- феноменологический параметр. Его величина

        до сих пор не нашла удовлетворительного теоретического объяснения.
        С физическим содержанием модели читатель может познакомится в
        работах

        Отметим только, что данная схема позволяет добавлять и другие
        новые кварки, но исключительно парами. Так, пару

        можно считать

        дублетом (
        и
        синглеты), причем компоненты

        мультиплетов

        соответствуют смеси, вообще говоря, всех трех кварков

        Впрочем, это смешивание невелико, поэтому далее мы его не
        учитываем.

        Подчеркнем, что формирование мультиплетов

        призвано прежде всего удовлетворить экспериментальным фактам, при
        этом набор кварков вслед за лептонами расщепился на три поколения:

        Таким образом, унификация всех видов взаимодействий (исключая
        гравитационное) должна основываться на существовании трех
        поколений фундаметальных частиц:

        Они состоят из синглетов (лептоны) и триплетов (кварки)
        относительно цветной группы

        Относительно

        как уже отмечалось, левые состояния этих частиц реализуют дублеты,
        а правые --- синглеты. Для каждого поколения нетрудно написать
        разложение по представлениям группы

        Например,

        Эти мультиплеты должны содержаться в представлении (желательно
        неприводимом) группы симметрии

        единой теории. Группа

        должна быть простой (только в таком случае в теории возможна
        единственная константа взаимодействия). Ранг минимальной такой

        равен рагну группы

        т. е. четырем. Минимальными (по размерности) простыми группами
        ранга 4 и более являются
        и
        Модели объединенной теории сильного, слабого и электромагнитного
        взаимодействия, основанные на этих группах симметрии, в настоящее
        время наиболее популярны.

        Проанализируем

        классификацию фундаментальных фермионов

        (остальные `` поколения '' рассматриваются совершенно аналогично).
        Они не укладываются в неприводимое представление, что вынуждает
        перейти к совокупности длух фундаментальных представлений этой
        группы. Обратимся к

        содержанию 5-плета и 10-плета

        Как видим это не совсем совпадает с разложением

        Соответствие восстановится, если к набору частиц

        добавить разложение для набора из античастиц. Можно убедиться, что
        им взаимно-однозначно сопоставляется содержание мультиплетов

        {Упражнение}.
        Заполните указанные мультиплеты частицами и античастицами
        первого поколения.

        {Замечание}.
        При заполнении представлений

        неизбежно приходится наряду с кварками включить в

        антикварки. В результате приходим к несохранению барионного числа

        {Замечание}.
        Для моделей, подобных

        характерно выделение одного `` поколения '' --- существование
        других фактически рассматривается как проявление некоего
        вырождения.

        Унитарная

        симметрия присутствует здесь лишь в неявном виде. Она, разумеется,
        становится явной в более широких схемах (включающих все
        `` поколения '' фундаметальных ферминов).

        Таковы характерные особенности современного статуса унитарной
        симметрии, т. е.: а)

        следует рассматривать как локальную (калибровочную) симметрию

        б) элементарные объекты, реализующие фундаментальное представление
        группы симметрии, суть кварки; другие неприводимые представления
        реализуются связанными состояниями кварков; в) число кварков
        больше трех, а соответствующая симметрия

        шире, чем

        г) в единой теории

        не выдвигается на первый план.


        {Гипотеза кваркового строения адронов. Массовые формулы и теорема
        Вигнера-Эккарта}

        Изложим кварковую модель строения адронов. Исходный постулат:
        наблюдаемые адроны являются связанными состояниями кварков и
        антикварков, реализующих фундаментальные представления
        и
        группы

        Тем самым для кварков зафиксированы значения наблюдаемых
        и
        (см. табл.
        ).
        Барионный заряд

        коммутирует с

        поэтому он одинаков для всех кварков.

        Преобразовательные свойства их связанных состояний

        должны совпадать со свойствами тензорного произведения
        представлений группы симметрии на свободных состояниях:

        На вопросе о существовании состояний

        свободных кварков (антикварков) останавливаться не будем.

        Вне радиуса действия сил, связывающих кварки, состояния

        практически ничем (а в случае их стабильности --- обсолютно ничем)
        не отличаются от состояний элементарных объектов. Следовательно,
        в совокупности

        нас прежде всего интересуют пространства, неприводимые
        относительно группы симметрии. Таким образом, кварковая модель
        адронов в качестве одной из основных содержит задачу разложения
        тензорного произведения представлений группы симметрии на
        неприводимые.

        Подчеркнем, что сказанное относится и к группе
        пространственно-временной симметрии частиц --- группе Пуанкаре. Ее
        квантовомеханические представления будут построены в гл. 9.

        В задаче разложения этих представлений

        сейчас более важной является часть, относящаяся к описанию
        внутреннего углового момента связанного состояния --- спина
        составной частицы. Поскольку подгруппа вращений

        общая для групп симметрий Пуанкаре и Галилея, воспользуемся
        известным читателю правилом сложения углового момента в
        нерелятивистской квантовой механике.

        Итак, полагаем, что состояния кварков реализуют представление
        группы вращений

        в котором операторы углового момента имеют вид

        где

        (неприводимые) операторы собственного углового момента кварка
        (спина), а

        операторы орбитального. Совместим начало отсчета с центром масс
        системы кварков (и антикварков). В этом случае оператор полного
        момента

        совпадает с оператором собственного углового момента адрона.

        Известно, что низшим (по энергии) состояниям, скажем, в атомах,
        соответствуют полные орбитальные моменты

        равные нулю:

        Можно полагать, что тем же свойством обладает и связная система
        кварков. Значениям

        соответствуют болле высокие по энергии (а следовательно, и по
        массе) состояния. Для простоты ограничимся рассмотрением случая

        тогда собственный угловой момент адрона составляется лишь из
        спинов кварков:

        Для последних разумно выбрать минимальное значение

        Мезонные состояния обладают целым спином, т. е. должны быть
        составлены из четного числа фундаментальных частиц. При этом
        поскольку их барионный заряд должен быть нулевым, число кварков и
        антикварков в них должно быть одинаковым. Простейшей такой
        системой является двухчастичная:

        Тогда (при

        )возможны два значения спина

        -свойства этих состояний определяются разложением
        (см. рис
        )

        В центре диаграммы унитарного октета мы выделили нормированную
        компоненту изотриплета

        изосинглет

        Соответствующие

        состояния можно отождествить с нонетом псевдоскалярных мезонов при

        и нонетом векторных мезонов

        при

        если установить их

        -четность.

        Пространственная четность

        определяется орбитальной четностью

        связанного состояния и произведением внутренних четностей.
        Последнее для частицы и античастицы спина

        равно

        В результате имеем

        что и наблюдается у нонетов

        -четность системы частица --- античастица выражается равенством

        где

        значение суммарного внутреннего момента кварков. (Элементарный
        вывод этого соотношения см., например, в
        )
        Для нейтральных компонент в

        получаем значения

        -четности, согласующиеся с экспериментально известными свойствами
        нонетов

        {Замечание}.
        Формулы

        показывают, что в кварковой модели в мезонных состояниях не
        появляются некоторые комбинации

        Такие комбинации называют `` экзотическими ''. Отметим, что
        состояний с экзотическими квантовыми числами обнаружить не
        удалось.

        {Замечание}.
        В разложении

        синглетное сотояние и одна из нейтральных компонент октета
        (восьмая) являются суперпозициями одних и тех же комбинаций
        кварков. Если существуют взаимодействия, нарушающие симметрию, то
        нет оснований полагать, что в реальные состояния эти комбинации
        входят именно с указанным в

        весом. Физические состояния, диагонализирующие полную энергию,
        вклад в которую дают и потенциалы упомянутых взаимодействий, могут
        оказаться суперпозицией (`` смесью ''), скажем, синглета и
        некоторой компоненты мультиплета (тогда говорят, что `` существует
        смешивание''). Отметим, что речь может идти лишь о нейтральных
        компонентах: смешивание остальных запрещено суперотбором по
        центральным наблюдаемым из подалгебры Картана группы симметрии.

        Обратимся к рассмотрению барионов. Минимальное нетривиальное число
        связанных кварков, обеспечивающих полуцелое значение спина, равно
        трем. Если барион составлен исключительно из кварков, то барионный
        заряд кварка

        В данном случае одна из основных теорем квантовой теории поля
        (теорема о связи спина и статистики. --- См., например,
        )
        приводит к нетривиальным ограничениям, которые заключаются в
        требовании антисимметричности волновой функции рассматриваемых
        состояний относительно перестановки пары кварков.

        Представим волновую функцию в виде

        Здесь мы выделили координатную часть

        и волновые функции

        в унитарном и спиновом пространствах. Для низших по массе
        барионных состояний по-прежнему положим

        Волновые функции

        при

        следует считать симметричными относительно перестановок координат
        кварков, поскольку предположение антисимметричности

        при

        приводит к появлению некоторых свойств адронов, не подтверждаемых
        экспериментально.

        При симметричной

        волновые функции в унитарном и спиновом пространствоах  должны
        быть в целом антисимметричными. Тогда, скажем, в состоянии с
        полным спином

        спиновая волновая функция полностью симметрична. Следовательно,
        унитарная волновая функция должна быть антисимметризована. Но в
        разложении представления

        полностью антисимметрично лишь синглетное состояние (см. пример
        ),
        что резко противоречит реальному положению вещей: состояния спина

        образуют дикуплет

        Напомним, что такой мультиплет в произведении

        полностью симметричен.

        Таким образом, чтобы привести наивную кварковую модель в
        соответствие с реальной системой состояний, необходима
        модификация. Она основывается на предположении существования трех
        дополнительных кварковых степеней свободы (цветных), реализующих
        представление

        группы

        Последняя входит в группу симметрии сильных взаимодействий

        прямым множителем. Следовательно, неприводимые представления

        имеют вид

        где

        неприводимы. Цветные степени свободы в реальных физических
        состояниях ненаблюдаемы, т. е. последние `` бесцветны ''. (Есть
        основания считать, что это свойство может быть получено как
        следствие в калибровочной теории

        с сильной связью.)

        Пусть

        волновая функция кварка в цветном пространстве. Бесцветность
        состояний адронов означает, что они являются синглетами
        относительно

        Тогда наличие цветных степеней свободы практически не вносит
        изменений в кварковую картину строения мезонов. В разложении

        достаточно каждую унитарную волновую функцию дополнить цветной и
        свернуть у них индексы. Например,

        В случае барионов единственный

        синглет в произведении

        антисимметричен. Таким образом, волновую функцию

        следует дополнить множителем

        В результате приходим к требованию совместной симметричности
        унитарной и спиновой частей относительно перестановок кварков. Для
        спина

        это дает единственное неприводимое представление

        декуплет. Обозначая операцию симметризации символом

        имеем

        Здесь для удобства установления кваркового состава каждой частицы
        декуплета

        мы добавили выражение

        В случае

        спиновая часть

        обладает смешанной симметричностью (см. пример
        ).
        Нетрудно проверить, что каждой октетной комбинации соответствует
        только одна спиновая, приводящая к итоговой симметричности
        спиновой и унитарной частей волновой функции. Барионам

        можно сопоставить любой из этих октетов. (В рамках
        нерелятивистской

        схемы появляются естественные основания рассматривать только один
        из них
        .)

        Итак, в предположении существования цветных степеней свободы
        кваковая модель способна описать состояния адронов в интервале
        масс

        для значений спинов

        Как видим, представления типа

        при этом действительно не появляются. Другие адронные состояния
        также представимы системами из двух или трех кварков (с
        нетривиальным орбитальным моментом
        )
        Однако, как выяснилось, все-таки не все --- потребовалось ввести
        новые ароматы.

        Ограничимся рассмотрением строения мезонных состояний с симметрией

        Пусть кварки

        реализуют фундаментальное представление

        группы

        Тогда мезоны

        образуют представление

        этой группы:

        В скобках здесь проставлены символы частиц со спином

        скалярных мезонов

        синглетное состояние не выделено. Эта же диаграмма описывает

        представление

        на векторных

        мезонах. Проставить символы частиц на диаграмме читателю не
        составит особого труда.

        До сих пор вопросы, связанные с нарушением рассматриваемых
        симметрий, практически не затрагивались. Между тем в физике оно
        играет, пожалуй, более важную роль, чем идеальная симметрия.

        Например, для унитарной

        симметрии возможность различия эквивалентных состояний связана не
        только с наличием наблюдаемых (скажем,
        ),
        различающих эти состояния, но и с существованием взаимодействий,
        чувствительных именно к этим наблюдаемым. (В данном случае это
        соответственно умеренносильное и электромагитное взаимодействия).
        В результате реальные состояния, которые должны быть
        `` диагональны '' для всей совокупности взаимодействий,
        характеризуются не только различными значениями наблюдаемых, но и,
        вообще говоря, разной собственной (внутренней) энергией, т. е.
        массой.

        Таким образом, расщепление масс частиц одного мультиплета группы
        внутренней симметрии является естественным следствием того, что ее
        нарушение также имеет внутреннюю природу. Из приведенных
        рассуждений следует, что наблюдаемая

        масса (или ее квадрат
        )
        во внутреннем пространстве является некоторым элементом
        обертывающей алгебры группы внутренней симметрии.

        Разумеется, нарушение этой симметрии проявляется не только в
        расщеплении масс, но и в различии значений других наблюдаемых
        (например, магнитных моментов). Здесь мы ограничимся рассмотрением
        массовых соотношений.

        Точное определение

        как элемента обертывающей алгебры по существу эквивалентно точному
        решению квантовополевых уравнений для взаимодействующих
        элементарных частиц (кварков). При феноменологическом описании
        частиц достаточно сосредоточить неизвестные нам аспекты динамики в
        нескольких параметрах и установить соотношения, которые с точки
        зрения (нарушенной) симметрии должны выполнятся во всяком случае.

        Относительно малая интенсивность электромагнитного взаимодействия
        обусловливает незначительность его влияния на величину масс
        адронов. Действительно, массы частиц, различающихся только
        электрическим зарядом (т. е. принадлежащих одному изомультиплету),
        можно считать одинаковыми. В таком случае оператор

        при учете лишь сильного и умеренно-сильного взаимодействий должен
        коммутировать не только с подалгеброй Картана

        но и с остальными генераторами изоспиновой подгруппы

        Следовательно, в обертывающей алгебре группы

        это элемент вида

        где

        функция только операторов Казимира

        рассматриваемой группы, а

        коммутирует лишь с

        Для простоты предположим, что

        тензорный оператор (см.
        ),
        преобразующийся, как оператор гиперзаряда

        восьмая компонента октета.

        В общем случае такие элементы обертывающей алгебры формально
        представимы производными

        от инвариантного элемента

        по генератору

        Воспользовавшись тем, что

        от

        зависит лишь через операторы Казимира

        а также явным выражением (см.
        )
        для последних, можем записать

        Здесь
        и
        функции операторов Казимира рассматриваемой группы. Имея в виду
        кварковую структуру адронов, следует предположить, что
        и
        могут зависеть также от спина

        частиц мультиплета, полного орбитального момента кварков

        и других наблюдаемых. Таким образом,

        суть феноменологические параметры, вообще говоря, свои для каждого
        мультиплета

        Выразив величину

        через значения изоспина

        и гиперзаряда

        в результате для

        получим выражение

        которое с хорошей точностью описывает массы барионных состояний. В
        случае мезонов формула

        должна быть записана для оператора квадрата массы

        {В рамках теории калибровочных полей механизм спонтанного
        нарушения симметрии приводит именно к линейным формулам масс для
        барионов и квадратичным --- для мезонов (см., например
        ).}
        Необходимо, кроме того, учесть, что формулы вида

        пригодны только для состояний одного мультиплета, тогда как
        некоторые из реальных мезонных состояний могут представлять собой
        набор компонент мультиплета и синглета (см. замечание
        ).
        Следовательно, в этом случае появляется еще один неизвестный нам
        параметр, описывающий смешивание. Пусть, например,

        физическое состояние

        мезонов
        и

        Представим их суперпозицией синглета

        и компоненты октета

        При вычислении матричных элементов

        для физических состояний достаточно дополнительно учесть, что
        функция

        для синглета, вообще говоря, тожет принимать иное значение, чем
        для октета. В результате для мезонных нонетов

        имеем столько же параметров, сколько и независимых
        изомультиплетов, в том числе параметр смешивания

        Обсудим предположение

        Первая часть его, согласно которой оператор

        тензорный, означает, что

        принадлежит конечномерному представлению

        и носит физический характер. На основании второй в разложении
        упомянутого представления предполагаем существенной лишь октетную
        компонету. Для оценки такого приближения выясним, какие
        представления содержатся в этом разложении в самом общем случае. С
        этой целью обратимся к теореме, чрезвычайно полезной для
        вычисления матричных элементов тензорных операторов.

        {Теорема Вигнера-Эккарта.}
        Матричные элементы неприводимого тензорного оператора

        между базисными векторами состояния

        неприводимых унитарных представлений

        компактной группы

        в пространствах

        имеют вид

        где

        коэффициенты Клебша-Гордана;

        скалярные функции, а суммирование распространяется на те (и только
        те) представления

        в разложении

        которые эквивалентны представлению

        (Инвариантые относительно

        величины

        принято называть редуцированными матричными элемнтами тензорного
        оператора.)

        Доказательство основано на инвариантности тензорных орераторов
        (см.
        )
        относительно преобразования:

        (Без ограничения общности представление

        в условии теоремы можно считать унитарным и значок опустить:
        .)

        Если матричные элементы сужения

        на

        обозначить через

        то для матричного элемента в

        из соотношения

        вытекает

        Ввиду фактической независимости правой и левой частей этого
        соотношения

        от

        напрашивается интегрирование обеих его частей по группе.
        Предварительно следует воспользоваться разложением

        (Здесь и далее указано суммирование лишь по символу, нумерующему
        представления. Суммирование по другим повторяющимся индексам
        подразумевается.) Тогда в силу соотношения ортогональности
        матричных элементов неприводимых представлений группы

        (теорема
        )
        в сумме по

        останутся лишь слагаемые для представлений

        эквивалентных представлению

        В результате получим искомую форму

        где

        {Следствие}.
        Если группа

        в теореме

        однократно приводима, то рассматриваемый там матричный элемент
        пропорционален коэффициенту Клебша-Гордана:

        Итак, по теореме Вигнера-Эккарта в разложении тензорного
        оператора

        содержатся те неприводимые представления

        для которых коэффициенты Клебша-Гордана в правой части соотношения
        вида

        отличны от нуля. Нетрудно установить, исходя из свойств указанных
        коэффициентов, что в этом (и только в этом) случае представление

        должно содержаться в произведении представлений

        Например, для октета барионов

        в массовом операторе существенны компоненты из неприводимых
        представлений

        Требованию коммутативности с

        удовлетворяет лишь

        Следовательно, предположение, что

        (его называют {октетной доминантностью}) эквивалентно отбрасыванию
        компоненты из

        В этом случае массовая формула

        предсказывает одно соотношение для масс изомультиплетов

        В случае декуплета барионов

        октетная доминантность (отбрасывание
        и )
        приводит к возможности получения двух предсказаний на основе
        формулы

        (одно из них --- значение массы

        гиперона, см.
        ).

        В заключение отметим, что по такой же схеме можно рассматривать
        соотношения масс в любой группе внутренней симметрии. В частности,
        в случае

        естественным обобщением октетной является 15-плетная
        доминантность. Направление в алгебре Ли

        `` вдоль '' которого расщеплены массы, есть комбинация элементов

        подалгебры Картана, коммутирующих с изоспиновой подгруппой

        Определение элемента обертывающей алгебры

        с соответствующими свойствами повторяет процедуру, проделанную для

        и в рамках данной схемы не содержит ничего принципиально нового.

        Мы рассмотрели некоторые особенности описания симметрии при
        квантовомеханическом подходе к теории элементарных частиц.
        Взаимопревращение последних, проявляющееся в их взаимодействии,
        обусловливает переход к формализму втричного квантования ---
        квантовой теории поля. Эта теория должна основываться на группе
        Пуанкаре релятивистской симметрии.


