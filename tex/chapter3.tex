        ГЛАВА 3

        ТОПОЛОГИЧЕСКИЕ ГРУППЫ И ГРУППЫ ЛИ

        Свойства групповых операций в топологических группах



        Группа физической симметрии, как правило, является группой
        движений топологического пространства. Это позволяет ввести
        естественное понятие близости для ее элементов и тем самым
        наделить группу

        структурой топологисеского пространства. Непрерывность
        преобразований физической симметрии должна быть согласована с
        топологической структурой на

        Для этого оказывается достаточным потребовать, чтобы групповое
        умножение и переход к обратному элементу

        были непрерывными операциями в искомой топологии.

        В главе

        мы установили, что группа симметрии обеспечивает нас наблюдаемыми,
        если, во-первых, окрестность единичного элемента

        отождествляется с окрестностью

        и, во-вторых, векторы ее касательного пространства в точке

        являются генераторами однопараметрических подгрупп. Выделяемая
        благодаря такому свойству совокупность топологических групп
        известна под названием групп Ли. Некоторые их свойства являются
        общими для всего класса топологических групп. Этот класс сначала и
        рассмотрим.

        Итак, {топологическая группа} --- это множество

        являющееся группой и одновременно топологическим пространством,
        причем групповые операции суть непрерывные отображения:

        для умножения и

        для операции

        Другими словами, пусть

         --- наперед заданная окрестность элемента

        Тогда можно указать окрестности

        такие, что

        Далее, для любой окрестности

        элемента

        всегда найдется

        такая, что


        {Замечание}.
        В определении

        условия

        можно заменить требованием непрерывности отображения


        Группу

        называют компактной, локально компактной, связной, односвязной и
        т. д., если

         --- топологическая группа, являющаяся компактным (соответственно
        локально компактным, связным, односвязным и т. д.) пространством.

        {Примеры}.
        Множество

        Вещественных чисел является (аддитивной) группой и, как известно,
        топологическим пространством. Отображение

        непрерывно.

        Группа

        является множеством обратимых матриц в

        Последнее, как известно, ---
        -векторное пространство размерности

        Фундометальные системы окрестностей точек


        Аналогично определяются

        Групповые операции непрерывны. (Покажите это.)

        Группу

        мы отождествили с группой унимодулярных кватернионов

        Множество последних совпадает со сферой

        топологическая структура которой индуцировани из

        Следовательно, топологию на

        можно ввести, потребовав,чтобы изоморфизм множеств

        был гомеоморфизмом. Непрерывность групповых операций нетрудно
        доказать, исходя из закона умножения:

        где


        Из определения топологической группы

        следует, что сдвиги

        и инволюция

        являются гомеоморфными отображениями

        Поэтому:

        подмножества

        открыты (или замкнуты), если открытым (или замкнутым) является
        подмножество

        локальные свойства группы достаточно задавать на окрестности
        одного (любого) элемента, в частности

        Например:

        группа

         --- {локально компактна}, ибо точка

        обладает окрестностью

        (см.

        ), замыкание которой компактно;

        топологическая группа

        тогда и только тогда {дискретна}, когда для единичного элемента
        допустима окрестность

        в любой окрестности единицы

        содержится такая

        что

        (возьмите в

        );

        в любой окрестности

        содержится симметричная

        т. е. вместе с каждым элементом

        содержащая и обратный к нему:





        Подгруппы, нормальные подгруппы, факторгруппы, естественные
        отображения, гомоморфизмы топологических групп. Прямые
        произведения

        Пусть

         --- топологическая группа и

         --- подгруппа в группе

        подгруппу

        (или нормальную

        подгруппу

        ) назовем {торологической подгруппой} группы

        (соответственно {нормальной топологической подгруппой}), если

        одновременно является замкнутым подмножеством в топологическом
        пространстве


        {Замечание}.
        В топологической группе любая

        подгруппа является топологической группой в индуцированной
        топологии. В частности, все классические группы из табл.

        реализуются как

        подгруппы

        и потому являются топологическими. Если индуцированная топология
        на подгруппе

        оказывается дискретной, то

        называется {дискретной подгруппой} группы


        Простейшей иллюстрацией нетривиальности указанных определений
        является группа

        гомеоморфная тору

        Здесь

         --- диагональные

        матрицы

        Подгруппа

        называется {рациональной обмоткой тора} при

        и {иррациональной обмоткой тора} в противном случае. Исследование
        свойств обоих типов

        предлагаем читателю провести самостоятельно.

        {Теорема}.
        Пусть

         --- топологическая группа. Тогда:

        а) если

        подгруппа (или нормальная

        подгруппа) в

        то ее замыкание

        также является

        подгруппойт (нормальной

        подгруппой) в группе

        б) если

        подгруппа

        является открытым подмножеством в


        {Доказательство}.
        а) Покажем, что

        для элементов

        Пусть

         --- окрестность элемента

        Тогда в

        существуют окрестности

        такие, что

        Эти окрестности должны иметь нетривиальное пересечение с

        поэтому существуют

        такие, что

        Поскольку

        то любая окрестность элемента

        имеет непустое пересечение с

        б) Одновременно с

        открытым подмножеством в

        является каждый класс смежности

        Объединение всех отличных от

        смежных классов есть также открытое подмножество в

        Но оно является дополнением

        следовательно,

         --- замкнуто.

        {Примеры}.

         --- замкнутая подгруппа.

         --- замкнутая подгруппа в

         --- в

        (соответственно в
        ).

         --- в

        (соответственно в
        ).

        Подгруппа

        открыта и замкнута одновременно.

        {Топологическая факторгруппа}

        топологической группы

         --- это факторгруппа группы

        по нормальной

        подгруппе

        и факторпространство топологического пространства

        по замкнутому в

        подпространству

        Фактортопология по существу согласована с групповым умножением в
        факторгруппе, поэтому

         --- топологическая группа.

        При факторизации

        с общем случае (т. е. когда

        не обязательно нормальная подгруппа и, возможно, незамкнутая)
        очевидны следующие свойства:


        факторпространство

        топологической группы

        однородно. (Напомним, что топологическое пространство

        называется однородным, если для любой пары его элементов

        найдется непрерывное отображение

        при котором

        .)

        Отображение факторизации является непрерывным и открытым, но
        сохранять свойства отделимости не обязательно. Действительно, при
        факторизации по

        подгруппе

        не являющейся ни открытым,ни замкнутым подмножеством, точка в

        в фактортопологии не будет ни открыта, ни замкнута, ибо прообраз
        замкнутого (открытого) множества должен быть замкнут (открыт).

        Свойства компактности наследуются при факторизации по
        топологическим подгруппам. Если

         ---компактна (или локально компактна), то пространства

        тоже компактны (соответственно локально компактны).

        {Пример}.
        Все факторпространства (и факторгруппы) группы

        по топологическим подгруппам локально компактны.

        Пусть

         --- топологическая группа. Если ее подгруппа

        компактна и пространство

        также компактно (или хотя бы локально компактно), то сама группа

        компактна (соответственно локально компактна), поскольку
        гомеоморфна прямому произведению

        топологических пространств.

        Возьмем в качестве примера

        Рассмотрим орбиту

        группы

        в

        -модуле

        содержащую какую-либо точку

        Для простоты выберем вектор

        Отметим, что группе стабильности этой точки соответствует группа

        поэтому

        -модули

        изоморфны.

        Из ортогональности преобразований

        следует, что все точки

        орбиты

        принадлежат сфере

        в пространстве

        Чтобы установить совпадение

        достаточно показать, что любую точку сферы

        можно представить в виде

        Если

        имеет компоненту

        эта задача тривиальна: все остальные компоненты

        и в качестве искомого

        можно взять преобразование отражения четного числа осей в

        включающего ось

        поэтому положим


        Каждому

        -компонентному вектору

        сопоставим антисимметричную матрицу:

        Взяв в качестве

        вектор с компонентами

        нетрудно проверить, что матрица Клейна

        выполняет искомое преобразование


        Итак,

        и сфера

        изоморфны как множества и как

        -модули. Одновременно они являются топологическими пространствами.
        Введенная с помощью

        топология в

        индуцирует топологию в замкнутой ее подгруппе

        фактортопология которой и определяет на

        структуру топологического пространства. Эта структура эквивалетна
        индуцированной на сфере

        в чем нетрудно убедиться, используя элементы

        в качестве представителей классов


        Сфера

         --- компактное топологическое пространство. Исходя их
        вырожденного случая

        индукцией по

        получаем, что все группы

        компактны.

        Дословное повторение сказанного применительно к случаю

        позволяет прийти к выводу о гомеоморфизме

        и компактности всех групп


        Группа

        является прямым произведением

        Из свойства

        следует, что

        компактна.

        Здесь же напомним, что группа

        (см. табл.

        ) является подгруппой в

        Множество ее элементов выделяется дополнительным условием
        сохранения симплектической формы с матрицей

        (см.

        ), значит,

        замкнута и поэтому компактна.

        Пусть

         ---  топологические группы. {Гомоморфизмом топологиических групп}
        называется непрерывный

        гомоморфизм групп

        Следует учесть, что топология в образе

        вообще говоря, может оказаться слабее индуцированной отображением

        поэтому основную роль играют открытые гомоморфизмы, в частности
        {изоморфизм топологических групп} --- групповой изоморфизм,
        являющийся гомеоморфизмом топологических пространств.

        Только для открытых гомоморфизмов имеет смысл сформулировать
        теорему, повторяющую теорему

        и ее утверждение будет справедливо.

        В качестве примера предлагаем читателю рассмотреть отображение


        Эта же группа

        иллюстрирует понятие {прямого произведения топологических групп}.
        Определим его как топологическое пространство

        с топологией произведения и групповой структурой --- прямым
        произведением групп. Групповые операции в

        непрерывны. Таким образом,

         --- топологическая группа.

        Отметим очевидное свойство: проекторы

         --- открытые гомоморфизмы.



        Многообразия: гладкость, координаты, локальная размерность,
        карты, атласы. Группы Ли. Параметризация. Общая линейная группа и
        классические группы как группы Ли

        В главе

        подчеркивалось, что нас интересуют группы с гладкой окрестностью
        единичного элемента.

        Исходное понятие --- гладкая зависимость функций на
        рассматриваемом множестве (в данном случае пусть это будет
        окрестность

        ). Непрерывная функция

        называется гладко\footnote{Здесь и далее для краткости термин ``
        гладкость '' употребим для обозначения как аналитичности, так и
        собственно гладкости.} зависящей (т. е. аналитически,

        -дифференцируемо или

        -кратно дифференцируемо зависящей) от непрерывной функции

        если для всех точек

        где функция

        называемая выражением

        через

         --- соответственно аналитична,

        -дифференцируема или

        -кратно дифференцируема в точке

        Подобным образом понимается и гладкая зависимость

        от нескольких функций


        Гладкость

        означает, что можно указать класс функций

         --- в соответствующих случаях), определенных на

        такой, что:

        1)

        содержит любую функцию, гладко зависящую от конечного числа
        функций из


        2) существует набор координатных функций

        из

        называемый {системой координат} на

        гомеоморфно отображающий

        на некоторый куб в

        причем каждая функция из

        гладко зависит от

        на


        Окрестность

        тогда называется {кубической}, число

         --- {шириной куба} (относительно системы координат

        ), а число

         --- {локальной размерностью}


        Отметим, что

        является системой координат в любой точке из


        {Утверждение}.
        Для того чтобы набор функций

        выражающихся через координаты посредством функций

        также был системой координат на

        необходимо и достаточно, чтобы: 1)

        2) функциональный определитель

        при

        был отличен от нуля.

        Рассмотрим свойства группы глобально. Из однородности
        топологического пространства группы

        следует, что гладкая структура, задаваемая кубической окрестностью

        классом

        гладких функций и системой координат

        имеется во всех точках

        если только она имеется в точке


        Действительно, сдвиг на

        определяет

        как совокупность всех элементов

        вида

        и гладкие функции


        Пусть при этом гладкие структуры во всех  случаях, когда
        окрестности

        нетривиально пересекаются, согласованы, т. е. классы

        на пересечении

        совпадают (рис.
        ).

        Тогда топологическое пространство

        по определению есть гладкое многообразие. В силу утверждения

        локальная размерность во всех точках связного многообразия

        одинакова и называется {глобальной размерностью} (или
        {размерностью}) многообразия.

        Очевидно, что из требования

        вытекает следующее:

        если

         --- координаты точек

        группы

        принадлежащих соответственно гладким окрестностям

        то локальные координаты

        являются гладкими (класса

        или

        ) функциями локальных координат сомножителей.

        Нетрудно заметить, что верно и обратное: при условии

        топологическая группа

        наделяется структурой гладкого (класса

        или

        ) многообразия, с которой согласованы групповые операции. Такие
        группы

        и носят название {групп Ли} (соответственно {аналитических} или
        {дифференцируемых}).

        {Замечание}.
        Для исключения некоторых патологических случаев предполагается,
        что база топологии в

        счетна.

        Мы исходили из гладкости окрестности

        единичного элемента. Вместо

        всегда можно взять `` меньшую '' окрестность

        с наследственной гладкостью, и в частности системой координатный
        функций

        Напомним, что окрестность

        каой-либо точки многообразия и гомеоморфное отображение

        на область

        называются {картой} на многообразии в данной точке.

        Всевозможные сдвиги

        образуют покрытие картами всего многообразия

        и в частности окрестности

        причем в случае пересечения

        с какой-либо картой

        согласованы гладкие структуры, или если

         --- координатные функции соответственно на

        то для сужения

        на пересечение

        композиция

         --- гладкое отображение.

        Вследствие компактности куба

        из его покрытия

        можно выбрать конечное:

        Взяв в качестве

        окрестность

        являющуюся порождающим множеством, нетрудно убедиться, что из
        покрытия

        группы

        можно выбрать не более чем счетное подпокрытие

        Для компактной группы

        из него можно выбрать даже конечное.

        Напомним, что набор карт

        покрывающий все многообразие и такой, что для любых пересекающихся
        карт

        определяемое в

        отображение удовлетворяет требованию гладкости, называется
        {атласом} на многообразии.

        {Замечание}.
        При отождествлении гладкой окрестности

        с порождающим множеством

        мы ограничили себя классом групп, являющихся связными
        многообразиями. (Напомним, что связным топологическим
        пространством (многообразием) называется такое, которое
        непредставимо в виде объединения двух непустых непересекающихся
        замкнутых множеств.) В приведенном определении группы Ли свойство
        связности многообразия предполагать необязательно. Тогда в круг
        рассмотрения попадут и такие группы, как группа

        являющаяся объединением классов смежности

        (см.
        ).

        Введение локальных координам на группе Ли тесно связано с
        {параметризацией} данной группы. Локально, например, в

         --- это просто взаимно-обратные процедуры: координатные функции

        осуществляют гомеоморфное (и, следовательно, взаимно-однозначное)
        отображение

        Тогда отображение

        является параметризацией элементов из


        В качестве {пространства параметров} рассматриваемой группы
        возьмем минимальную область

        характеризуемую тем, что

        пробегает все элементы группы

        (хотя бы один раз), если

        пробегает всю

        Рассчитывать на то, что параметризация

        произвольной группы Ли во всех точках

        является гладким (или хотя бы гомеоморфным) отображением

        не приходится. Для примера здесь можно сослаться  на
        параметризацию  группы

        углами Эйлера (см. рис.
        )

        Она вырождена при

        Параметризация

        этой же группы теряет взаимную однозначность на границе шара при


        Предположим, что заданы группа симметрии

        и ее параметризация

        Пусть из физических соображений следует, что

        допускает (согласованную с групповой) структуру непрерывного
        многообразия класса

        и в частности, что ее параметризация определяет некоторую карту

        в окрестности

        единичного элемента, где

        гомеоморфно отображает

        на некоторую область

        пространства


        В этих условиях, как оказывается, нет необходимости специально
        проверять, является ли

        аналитической или хотя бы

        дифференцируемой группой Ли. По теореме Глиссона --- Монтгомери
        --- Циппина всякая топологическая группа со структурой

        -многообразия, согласованной с групповой, допускает структуру
        аналитической группы Ли.

        Мы не имеем возможности остановиться здесьна рассмотрении этой
        теоремы

        Для нас существен лишь вывод, что группы физической симметрии
        можно считать аналитическими группами Ли. В дальнейшем будем
        именовать их просто {группами Ли}.

        Отметим основные свойства, которые следуют из существования атласа
        аналитических координат, т. е. таких, что функции в правой части

        --- аналитические.

        Всякая

        подгруппа группы Ли, являющаяся замкнутым подмногообразием, сама
        есть группа Ли (назовем ее {подгруппой Ли}).

        Всякая факторгруппа группы Ли по нормальной подгруппе Ли ---
        группа Ли.

        Всякая группа Ли локально компактна и локально связна.

        Прямое произведение

        групп Ли является группой Ли. Проекторы

        --- аналитические отображения многообразий (см.
        ).

        Одной из важнейших групп Ли является группа

        Ранее для задания топологии в

        мы использовали пространство

        образованное наборами матричных элементов матриц

        Любую матрицу

        удобно представить в виде

        Тогда пространством параметров группы

        является все

        за вычетом подмногообразия меньшей размерности, выделяемого
        алгебраическим уравнением


        Любой элемент

        обладает кубической окрестностью

        Координатные функции

        гомеоморфно отображают

        на куб в

        подходящей ширины

        Отметим, что глобальные координаты произведения

        выражаются аналитически через координаты

        сомножителей


        Таким образом, группа

        является группой Ли. Аналогично комплексные координаты

        наделяют группу

        структурой комплексного многообразия, причем групповая композиция
        с нею согласована. Такие группы называются {комплексными} группами
        Ли.

        Группу

        можем рассматривать и как вещественную

        -параметрическую группу Ли с пространством параметров

        образованным вещественными и мнимыми частями матричных элементов


        Напомним, что классические группы из табл.

        являются подгруппами в

        (или в

        ). Условия, их выделяющие, имеют вид системы алгебраических
        уравнений для матричных элементов группы

        Следовательно, любая классическая группа является замкнутым
        подмногообразием в

        т. е. подгруппой Ли.

        Например, в случае группы

        упомянутые уравнения имеют вид


        Левые части уравнений

        являются комплексно-аналитическими функциями координат
        комплексного многообразия

        Следовательно,

        также может рассматриваться и как (вещественная) группа Ли, и как
        комплексная группа Ли.

        Это не относится, например, к группе

        поскольку левые части соответствующих уравнений

        не являются комплексно-аналитическими функциями. Но они
        представляют собой аналитические функции

        координат (вещественного) многообразия


        {Упражнение}.
        Укажите все группы из табл.

        являющиеся комплексными группами Ли.




        Связные компоненты топологической группы,

        Теорема о конечной порожденности. Свойства дискретных нормальных
        подгрупп. Компоненты группы Лоренца

        Перейдем к рассмотрению свойств группы физической симметрии,
        вытекающих из связности или несвязности ее многообразия. Напомним
        сначала, что {компонентой} элемента

        топологического пространства (топологической группы)

        называется максимально связное множество

        содержащее этот элемент (оно всегда замкнуто).

        {Утверждение}.
        Компонента единицы

        является нормальной топологической подгруппой группы


        {Доказательство}.
        Свойство замкнутости

        --- характеристическое свойство компоненты

        Необходимо показать, что

        --- нормальная подгруппа.

        Поскольку отображение

        является гомеоморфизмом

        то множество

        связно. Оно содержит

        а тогда

        в силу максимальности компоненты

        Следовательно,

        наряду с каждым элементом

        содержит и обратный к нему

        Далее пусть

        Сдвиг

        также является гомеоморфизмом, поэтому множество

        связно. Оно также содержит

        поэтому

        т. е.

        --- подгруппа.

        Наконец, для любого

        отображение

        --- также гомеоморфизм. При этом

        связно, содержит единицу

        и потому


        В силу однородности факторпространства

        его элементы (класса смежности) сами являются компонентами в

        Факторгруппа

        называется {группой компонент} топологической группы


        Свойство

        является характеристическим для {связной группы}

        Свойство

        характеризует {вполне несвязную группу}.

        {Упражнение}.
        Покажите, что группа компонент

        топологической группы

        вполне несвязна.

        В частном случае локально связной группы

        т. е. допускающей связную окрестность единицы

        (как известно, таковы группы Ли), справедливо более сильное
        утверждение.

        {Утверждение}.
        Группа компонент

        локольно связной топологической группы

        дискретна.

        В самом деле, образ

        при каноническом отображении

        с одной стороны, является точкой (ибо

        ), а с другой --- окрестностью единицы

        (поскольку каноническое отображение открыто).

        Рассмотрим теперь свойство связных групп, впервые установленное
        нами для группы


        {Теорема о конечной порожденности}.
        Связная топологическая группа порождается любой окрестностью своей
        единицы.

        {Доказательство}.
        Пусть

        --- окрестность единичного элемента связной топологической группы

        Рассмотрим подгруппу

        порожденную элементами этой окрестности. Группа

        --- объединение множеств вида

        В свою очередь каждое

        есть объединение множеств вида

        поэтому все

        вместе с

        открыты одновременно с

        Но тогда по теореме

        замкнута. Вместе с

        открытостью и замкнутостью обладает дополнение


        Если

        то группа

        является объединением непустых замкнутых множеств

        (поскольку

        заведомо непусто), что противоречит предположенной условием
        теоремы связности.

        Полезным критерием, позволяющим во многих случаях устанавливать
        связность той или иной группы, является следующая теорема.

        {Теорема}.
        Пусть

        --- топологическая подгруппа группы

        Если группа

        и факторпространство

        связны, то сама группа

        также связна.

        {Упражнение}.
        Докажите теорему


        В качестве примера применения этой теоремы рассмотрим случай

        В примере

        мы установили, что

        гомеоморфно сфере

        Последняя, как известно, связна при всех

        Поскольку в тривиальном случае

        эта группа содержит лишь один (единичный) элемент и безусловно
        связна, то следствием доказанной теоремы является такое
        утверждение.

        {Утверждение}.
        Все группы

        связны.

        {Упражнение}.
        Покажите, что группы

        связны.

        Чтобы сформулировать еще одно свойство связных групп, отметим, что
        в любой топологической группе

        ее центр

        всегда является замкнутым множеством, что следует из регулярности
        топологического пространства

        Подгруппы, принадлежащие центру

        будем называть {центральными}.

        {Теорема}.
        Любая дискретная нормальная подгруппа

        связной топологической группы

        является центральной нормальной подгруппой.

        {Доказательство}.
        В силу дискретности

        у любого элемента

        найдется окрестность

        не содержащая других элементов из

        кроме

        Вследствие непрерывности отображения

        как по

        так и по

        найдется окрестность единицы

        такая, что

        Тогда для любого элемента

        необходимо, чтобы

        в силу инвариантности подгруппы

        Значит,

        поскольку в

        других элементов из

        нет. Воспользовавшись теперь теоремой

        о конечной порожденности, приходим к выводу, что

        коммутирует со всеми элементами группы

        т. е. принадлежит ее центру.

        Итак, наиболее важное свойство связных топологических групп (и
        групп Ли) выражается теоремой о конечной порожденности. Она же
        лежит и в основе инфинитезимального метода теории групп.

        Для нас интерес представляют также несвязные группы Ли --- их
        усторойство описывается в утверждениях

        откуда следует, что в общем случае группа Ли

        является расширением некоторой дискретной группы

        с помощью связной группы

        --- компоненты единицы в

        Таким образом, для  задания порождающего множества

        группы

        необходимо к любой окрестности

        добавить образующие дискретной группы


        Возьмем, например, несвязную группу

        Указанное расширение является прямым произведением

        компоненты единицы

        на группу отражений

        Рассмотрим группу Лоренца

        (см.

        ). Топологическая группа является объединением замкнутых множеств
        --- собственной группы Лоренца

        и совокупности несобственных преобразований Лоренца

        (не составляющих группы!):

        причем

        не имеют общих элементов. В

        содержится столько же компонент, сколько и в

        ибо

        Отметим, что в качестве представителей классов смежности

        удобно выбрать тождественное преобразование

        и преобразование

        отражения пространственных осей, матричные элементы которого
        {численно} совпадают со значениями метрического тензора:


        Возьмем в пространстве Минковского

        точку

        и рассмотрим орбиту группы

        ее содержащую. Малой группой (группой стабильности) точки

        очевидно является группа

        чисто пространственных вращений. Все точки орбиты

        принадлежат гиперболоиду

        представляющему собой несвязное множество. Его компоненты

        выделяются дополнительными условиями


        В пространстве

        содержатся все точки орбиты собственной ортохронной группы Лоренца


        {Упраженеие}.
        Покажите, что точку

        можно перевести в любую точку

        подходящим преобразованием вида


        Итак, факторпространство

        и компонента гиперболоида

        изоморфны как множества. Используя матрицы преобразований

        в качестве представителей классов смежности, нетрудно убедиться,
        что индуцированная топология на

        эквивалентна топологии на


        Поскольку пространство

        связно и этим же свойством, как известно, обладает группа

        то по теореме

        собственная ортохронная группа Лоренца

        связна.

        Теперь нетрудно установить, что указанные в

        множества

        являются связными компонентами группы Лоренца


        {Упражнение}.
        Постройте таблицу умножения группы компонент

        В качестве представителя второго класса в

        полезно использовать преобразование

        полного отражения в






        Локальная группа, локальные изоморфизмы. Свойства локальных групп

        Исключительная роль, которую играет окрестность единицы

        в связной топологической группе, побуждает отвлечься от всех иных
        групповых отношений, кроме тех, что наследуются в

        Получаемый при этом математический объект, для задания которого
        необходимо указать топологическую группу (или группу Ли)

        и окрестность единицы

        называется {локальной группой} (или {локальной группой Ли}) и
        является классом эквивалентности по определяемому ниже отношению
        локального изоморфизма.

        Топологические группы (группы Ли)

        называются {локально изоморфными}, если существует такое
        гомеоморфное отображение

        (локальный изоморфизм) окрестности единицы

        на окрестность единицы

        группы

        которое удовлетворяет следующим требованиям:

        а) если в окрестности

        наряду с элементами

        содержится и их произведение

        то


        б) если элементы

        таковы, что

        то их произведение

        также принадлежит


        Обобщение локального изоморфизма --- {локальный гомоморфизм}

        (в определении

        вместо гомеоморфного отображения `` на '' следует взять
        непрерывное `` в '').

        Отметим, что

        обладает свойством, в соответствии с которым для

        элемент

        принадлежит

        во всех случаях, когда он содержится в

        Подгуппа

        в локальной группе

        пересечение которой с

        обладает тем же свойством, называется {локальной нормальной
        подгруппой}. Объединение в один класс смежности по

        элементов

        из

        для которых

        приводит к понятию локальной факторгруппы

        с естественным умножением классов смежности.

        Наконец, отметим, что при открытом локальном гомоморфизме

        существует локальный изоморфизм


        Рассмотрим установленный в

        гомоморфизм

        группы

        на группу

        Можно видеть, что его ограничение на окрестность

        соответствующую шару радиусом

        в пространстве параметров

        удовлетворяет требованиям


        {Замечание}.
        Группы

        связны (см. упражнение

        ), т. е. в этом примере локально изоморфны связные группы, отнюдь
        не изоморфные глобально.

        Проанализируем свойство локальных групп.

        {Теорема}.
        Пусть

        --- дискретная нормальная подгруппа топологической группы

        Тогда естественный гомоморфизм

        является локальным изоморфизмом.

        {Доказательство}.
        Вследствие дискретности

        найдется такая окрестность единицы

        что кроме

        она не имеет с

        других общих элементов. В соответствии с

        в

        содержится окрестность

        такая, что

        Рассмотрим сужение естественного гомоморфизма

        на окрестность

        Пусть

        --- ее образ. Предположим, что элементы

        имеют совпадающие образы

        Тогда элемент

        Но окрестность

        выбрана так, что

        Следовательно,

        так как

        --- единственная общая точка


        Таким образом, каноническое отображение

        будучи непрерывным и открытым, на

        к тому же взаимно-однозначно, т. е. является гомеоморфизмом

        Удовлетворение требований

        есть простое следствие гомоморфности


        Доказанная теорема обобщает случай локального изоморфизма,
        упомянутый в примере

        Действительно, как известно,

        где

        --- центр группы


        {Теорема}.
        Если

        --- локально изоморфные связные топологические группы, то
        существует топологическая группа

        с такими дискретными инвариантными нормальными подгруппами

        что


        Приведем схему доказательства. Пусть

        --- те окрестности единиц в группах

        --- то отображение, существование и соответствующие свойства
        которых предполагает локальный изоморфизм.

        Образуем множество

        пар элементов

        а также

        понимая под произведением пар следующее:


        Рассмотрим множество

        являющееся объединением всех

        Оно является группой и, как можно показать (подробнее об этом см.

        ), допускает введение топологии, причем так, что проекторы

        которые являются

        гомоморфизмами

        соответственно в

        являются гомоморфизмами топологических групп. Но во всевозможных
        парах

        встретятся все элементы группы

        на первом месте и все элементы

        --- на втором, ибо

        --- порождающие множества для этих групп (поскольку последние
        связны), т. е.

        Дискретность инвариантных подгрупп

        при этом является следствием того, что в окрестности

        отображения

        взаимно-однозначный, а значит,

        не содержит других элементов из подгрупп

        кроме единицы


        {Следствие}.
        Если в условии теоремы

        группы

        не только связны, но и локально связны, то: а) группа

        связна; б) нормальные подгруппы

        --- центральные.

        {Доказательство}.
        а) В отличие от просто связности локальная связность

        предполагает в каждой окрестности любой точки пространства

        наличие связной окрестности. Пусть в

        такой окрестностью единицы является

        Одновременно связны

        и окрестность

        порождающая

        Тогда

        принадлежит компоненте единицы

        Так как любой элемент

        представим (конечным) произведением элементов из

        а последние принадлежат подгруппе

        то

        т. е.


        Пункт б) есть следствие а) и теоремы


        Следствие

        особенно важно потому, что группы Ли (а именно они и представляют
        для нас интерес) локально связны.




        Однопараметрические подгруппы. Единственность однопараметрической
        подгруппы с заданным направляющим вектором. Канонические
        координаты

        рода

        Ограничим рассмотрение группами Ли. Предварительно напомним
        следующие определения.

        Пусть

        --- многообразия размерности соответственно

        а

        --- отображение

        Если локальные координаты

        любой точки

        принадлежащей образу

        являются гладкими (класса

        или

        ) функциями

        локальных координат

        точки

        то

        называется гладким отображением (соответствующего класса)
        многообразия

        Если при этом ранг матрицы

        во всех точках

        принимает одно и то же максимальное значение, то

        называется регулярным отображением. Если регулярное отображение

        инъективно (что возможно лишь при

        ), то оно называется вложением многообразия

        Если

        и к тому же

        отображает

        на все

        причем

        также регулярно, то такое вложение называется диффеоморфизмом.

        {Гладкой кривой} в группе Ли назовем гладкое отображение

        Локальные координаты

        точек кривой --- гладкие функции параметра

        Нас в основном будут интересовать кривые, проходящие через
        единичный элемент группы, поэтому отображение

        нормируем условием

        В свою очередь систему координат в

        также удобно подчинить требованию

        чтобы совместить ее центр с единицей группы. Иногда для
        обозначения  координат элемента

        будем использовать ту же корневую букву:


        Касательная к гладкой кривой (класса

        ) в точке

        определяется прямой в пространстве параметров, проходящей через
        точку

        с направляющим вектором

        Таким образом, с каждой точкой группы

        ассоциируется векторное пространство

        направляющих векторов к дифференцируемым кривым в

        проходящим через эту точку. Назовем его касательным пространством
        к

        в точке

        Эта конструкция эквивалентна обычно вводимому понятию касательного
        пространства к многообразию (более подробно об этом см., например,
        в
        ).

        {Однопараметрическая} подгруппа в

        --- это гладкая кривая, являющаяся гомоморфизмом топологических
        групп

        При соглашении о нормировке последнее требование принимает вид


        Ограничиваясь локальным рассмотрением, изучим ее свойства в
        пределах одной карты

        (проблема продолжения рассматривается, например, в
        ).

        {Замечание}.
        При указанном условии в определении однопараметрической подгруппы
        отображение

        достаточно считать локальным гомоморфизмом. Именно это в конечном
        итоге приводит к тому, что алгебра Ли (см.

        ) оказывается сопоставленной сразу всему классу групп Ли, локально
        изоморфных данной.

        Нетрудно показать

        что кривая

        удовлетворяет дифференциальному уравнению

        где

        а аналитические функции

        --- координаты элемента

        (см.

        ). Тогда известные теоремы анализа приводят к следующим выводам.

        Каждый направляющий вектор

        определяет однопараметрическую подгруппу группы Ли


        Существует только одна однопараметрическая подгруппа

        с заданным направляющим векторм


        Всякая однопараметрическая подгруппа

        группы Ли --- аналитическая кривая. Функции

        аналитично зависят от всех своих аргуметов.

        Однопараметрические подгруппы следует считать независимыми, если
        их направляющие векторы линейно независимы.

        Свойства

        сохраняются при аналитическом преобразовании координат


        Итак, совокупность однопараметричкских подгрупп группы Ли

        в области параметров

        (значений координат

        ) является пучком аналитических кривых, проходящих через начало. В
        этой точке у каждой кривой есть своя касательная прямая, и в
        области

        они представимы рядом Тейлора.

        Как известно, существует аналитическое преобразование координат,
        которое переводит такое семейство кривых в прямые во всей области

        Искомая система локальных координат группы Ли

        называется {канонической} (

        {рода}).

        Таким образом, если система координат

        --- каноническая, то ее основное свойство заключается в том,что
        любая однопараметрическая подгруппа

        имеет координаты

        где


        Рассмотрим примеры.

        В группе

        вводимые соотношением

        координаты

        канонические.

        В группе

        мы ввели аналитические координаты соотношением

        Экспонента

        осуществляет универсальный изоморфизм аддитивной

        и мультипликативной

        групп. Это наводит на мысль представить элементы

        в экспоненциальном виде.

        Экспоненту от матрицы

        определим сначала формальным рядом


        {Упражнение}.
        Покажите, что каждый из

        рядов для матричных элементов в правой части

        сходится абсолютно и равномерно для матриц

        из любой ограниченной области в


        Очевидно, что соотношение

        определяет обратимую матрицу

        поскольку ряд

        также абсолютно сходящийся, и перемножение рядов дает


        Таким образом, соотношение

        определяет отображение

        Покажем, что в действительности любой элемент

        представим в виде экспоненты

        от некоторой матрицы


        Приведем комплексную матрицу

        преобразованием подобия

        к нормальной форме

        где блоки

        соответствуют

        -кратно вырожденным собственным значениям

        и являются треугольными (для определенности верхними треугольными)

        матрицами с числами

        на диагонали:

        Поскольку собственные значения обратимой матрицы

        отличны от нуля, каждая такая матрица

        имеет вид

        где матрица

        нильпотентна: ее степень

        обращается в нуль:

        Очевидно, что

        поэтому в представлении

        матрица

        является полиномом степени не выше

        т. е. проблема сходимости не возникает, и матрица

        однозначно определена по


        Представив в экспоненциальном виде каждый блок, заметим, что и вся
        блочно-диагональная матрица

        представляется экспонентой:

        т. е.

        где


        {Замечание}.
        Вычислим определитель матрицы

        Соотношение

        в случае конечномерных матриц

        является тождеством. В квантовой теории поля при отсутствии
        конструктивного способа введения определителя оператора
        соотношение

        служит его определением.

        Для перестановочных матриц

        Следовательно, все кривые

        являются однопараметрическими подгруппами, а

        координат

        --- каноническими

        рода. (Напомним, что

        можно рассматривать и как комплексную группу Ли. Тогда
        канонические координаты --- комплексные числа
        .) В достаточно малой окрестности единицы

        эти координаты совпадают с координатами

        в

        поэтому функциональный определитель

        отличен от нуля (и равен

        при
        ).

        Нам понадобятся также канонические координаты

        рода --- их можно ввести, если известна совокупность
        однопараметрических подгрупп

        рассматриваемой группы Ли

        в какой-либо аналитической координатной системе

        Возьмем любые

        независимых подгрупп

        (т. е. отвечающих линейно независимым векторам

        ) и параметризуем элементы

        координатами

        положив

        Такие координаты

        называются {каноническими координатами}

        {рода}.

        {Упражнение}.
        Покажите, что старые координаты

        элемента в левой части

        являются аналитическими функциями параметров

        и функциональный определитель

        отличен от нуля.




        Подгруппы и факторгруппы в канонических координатах, группа
        Лоренца

        Проанализируем свойства подгрупп Ли группы

        представляющих собой замкнутые подмногообразия.

        Рассмотрим сначала подгруппу Ли

        как группу Ли, а затем свойства ее тождественного вложения в

        Как и любая группа Ли

        обладает аналитическими координатами

        где

        --- карта и

        --- размерность

        Пусть координаты

        --- канонические

        рода, т. е. любой элемент

        есть

        где

        --- совокупность независимых однопараметрических подгрупп.

        Установим теперь, как

        может быть вложена в группу

        Значение аналитических координат

        точек

        являются функциями координат

        Представив эти функции в виде

        можно последовательно показать, что они аналитически зависят от
        каждого

        и убедиться, что в окрестности

        ранг матрицы

        точно равен


        Отметим теперь, что все направляющие векторы

        однопараметрических подгрупп в подгруппе

        --- это те и только те

        которые одновременно лежат в касательном подпространстве в точке

        к подмногообразию в

        определяемому системой уравнений

        Таким образом, совокупность

        является линейным подпространством в

        (размерности

        ). Всегда существует такой базис в

        что его первые

        образующих являются базисом в

        а остальные ортогональны

        Тогда из определения

        канонических координат

        рода вытекает следующее утверждение.

        {Утверждение}.
        Пусть

        --- подгруппа Ли (размерности

        ) в группе Ли

        (размерности

        ). Тогда:

        а) в

        существуют такие локальные канонические координаты

        рода

        что при

        первые

        из них являются одновременно (локальными) каноничкскими
        координатами

        рода на подгруппе Ли


        б) отображение

        где

        вводит локальные аналитические координаты на однородном
        пространстве

        (

        очевидно является многообразием.)

        {Утверждение}.
        Если в условиях предыдущего утверждения подгруппа Ли

        --- нормальная, то определяемые отображением

        координаты являются локальными каноническими

        рода на группе Ли


        Полученные выводы удобно проиллюстрировать группой

        Канонические координаты на

        мы ввели с помощью экспоненциального представления

        Однопараметрические подгруппы определяются соотношением

        (Чтобы не путать направляющие векторы

        с координатами в группе, последние обозначаем строчными буквами:

        )

        Считаем

        комплексной группой. Подгруппа

        выделяется условием

        что в силу соотношения

        эквивалентно

        Очевидно, что

        --- локальная координата в (комплексном) многообразии

        В факторгруппе

        она же является (тривиальной) канонической

        рода. (В вещественной группе Ли

        --- две координаты:

        .)

        Очевидно, что все недиагональные матричные элементы

        войдут в число канонических координат

        Тогда в силу соотношения

        произвольный элемент

        имеет вид


        Очевидно, что локальными (причем существенно локальными)
        координатами подгруппы

        являются вещественные части координат группы

        Последнюю здесь необходимо считать вещественной (

        -параметрической).

        Требование сохранения формы

        над

        в экспоненциальном представлении матрицы

        имеет вид


        Подобная задача уже рассматривалась в

        для вещественного случая

        где был построен базис из линейно независимых матриц

        в пространстве направляющих векторов

        этой группы

        Представив с их помощью общее решение уравнения

        получим

        вещественных канонических координат

        группы

        Их продолжение на комплексные значения дает канонические
        координаты


        {Замечание}.
        Удвоение числа матриц

        и параметров

        позволяет придать выражению

        формально тензорный вид

        Здесь


        В качестве

        будут фигурировать те подгруппы, факторпространства

        по которым встречаются в расслоении

        на

        -однородные пространства.

        Очевидно, что любая орбита

        группы Лоренца в

        содержится в подмногообразии

        пространства

        определяемом уравнением

        (в действительности совпадает с ним).

        Начало координат

        само по себе является орбитой

        вследствие однородности преобразований

        Группой стабильности

        его единственной точки является вся группа

        Элементы группы

        обозначим здесь через

        (для матрицы формы

        используем введенное нами стандартное обозначение

        ).

        Уравнения

        выделяющие

        как подгруппу в

        в каноничкских координатах последней имеют вид

        Таким образом, матрица

        с нижними индексами удовлетворяет уравнению

        Следовательно, значения канонических координат

        на подгруппе

        можно представить в виде

        и шесть параметров

        взять в качестве каноничкских координат на

        Удобно удвоить их число (см. замечание

        ), положив

        и поменять знаки некоторых матриц

        в определении

        Введя для кординат

        в частных случаях обозначения

        выражение

        для значений координат

        можно записать в виде


        При

        уравнение

        определяет конус

        (рис.

        ), из которого мы должны выколоть точку

        В классе взаимно сопряженных подгрупп

        --- групп стабильности всевозможных точек

        --- нам достаточно рассмотреть одну. Выберем ее, руководствуясь
        простотой:


        Для того чтобы однопараметрическая подгруппа

        проходящая через элемент

        принадлежала группе стабильности

        точки

        необходимо

        т. е.

        Очевидно, что замена

        где

        приводит к координатам в

        которые обладают описанными в утверждении

        свойствами. Значения

        имеют координаты элементов из подгруппы

        Совокупность

        есть локальные координаты в окрестности


        При

        все орбиты являются двуполостными гиперболоидами. Достаточно
        рассмотреть одну из них, скажем,

        положив

        и взяв точку

        в виде

        (рис.

        ). Этот случай был рассмотрен в

        Остается только добавить, что координатами в группе

        очевидно являются

        Связь локальных координат

        с координатами точек гиперболоида

        (см.

        ) нетрудно установить из соотношения

        где


        Как и в предыдущем случае, из совокупности однополостных
        гиперболоидов, описываемых уравнением

        при

        достаточно выбрать один:

        Взяв в качестве стандартного вектор

        нетрудно отождествить его группу стабильности

        с группой

        Более подробное исследование этой орбиты

        мы предлагаем читателю провести самостоятельно.

        Итак, мы установили, что в каноничкских координатах все группы Ли
        локально имеют одинаковое строение: в окрестности точки

        пучок всех прямых, проходящих через нее, представляет собой все
        семейство однопараметрических подгрупп данной группы. Различие
        групп Ли фиксированной размерности

        состоит в том, что в разных группах Ли по-разному замыкаются ``
        параллелограммы ''

        составленные из отрезков однопараметрических подгрупп

        при

        Кроме того, возможны различия в глобальных свойствах
        многообразий, которыми являются группы Ли, а именно в связности и
        односвязности.



        Накрывающее пространство. Принцип монодромии. Универсальная
        накрывающая группа

        В замечании

        отмечалось, что алгебра Ли сопоставляется всему классу групп Ли,
        локально изоморфных данной. Обозначим его через

        В этом классе достаточно рассмотреть только связные грппы: все
        несвязные в соответствии с утверждениями

        реализуются как расширения дискретных групп с помощью связных
        групп Ли. Таким образом, возникает проблема описания класса
        локально изоморфных связных групп Ли (обозначим его
        ).

        Для любых двух связных групп

        теоремы

        и следствие

        утверждают существование связной группы

        такой, что

        где

        --- дискретные нормальные подгруппы в

        (принадлежащие ее центру).

        Прежде чем обсуждать проблему существования универсальной группы
        Ли

        сразу для всего класса

        рассмотрим свойства отображений вида


        Связное пространство (или

        -многообразие)

        для которого определено непрерывное (или

        -гладкое) отображение

        называется {накрывающим пространством (накрывающим
        многообразием)} пространства

        если любая точка

        обладает такой окрестностью

        что:

        1) полный прообраз

        есть объединение попарно непересекающихся компонент


        2) ограничение

        отображения

        на каждую компоненту

        является гомеоморфизмом (диффеоморфизмом)

        на всю

        При этом окрестность

        существование которой предполагает данное определение, называется
        {правильно накрытой} ( или {допустимой}) относительно

        а само отображение

        --- {накрывающим}. Каждая

        называется {правильно накрывающей компонентой}.

        Известным примером накрывающего отображения является

        (рис.
        ).

        Формула

        реализует накрывающее отображение

        многообразия

        на многообразие

        Из нее видно, что каждая точка

        обладает правильно накрытой относительно

        окрестностью

        и имеются точно две правильно накрывающие компоненты:
        принадлежащие им матрицы

        различаются знаком.

        Отметим, что число правильно накрывающих компонент одинаково для
        правильно накрытых окрестностей всех точек связного пространства

        Это число

        называется {числом накрытия}.

        Значением

        характеризуется гомоморфизм

        (см.

        ), также являющейся накрывающим отображением.

        Накрытия

        пространства (многообразия)

        назовем {эквивалентными}, если существует гомеоморфизм
        (диффеоморфизм)

        такой, что накрывающее отображение

        представимо композицией

        Например,

        и группа унимодулярных кватернионов

        --- эквивалентные накрывающие многообразия для


        Связное и локально связное пространство

        назовем {односвязным}, если при любом его накрытии

        число накрытия

        т. е.

        взаимно-однозначно и является гомеоморфизмом (диффеоморфизмом)

        (или любое накрывающее пространство односвязного пространства

        с точностью до эквивалентности совпадает с ним самим).

        Односвязное накрывающее пространство

        для произвольного связного

        называется также {универсальным накрывающим}. Решение проблемы

        сводится к решению следующих задач.

        Необходимо установить, допускает ли пространство группы

        представляющей класс

        универсальное накрывающее


        Выяснить возможность восстановления на пространстве

        групповой структуры, при которой накрывающее отображение ---
        гомеоморфизм групп


        Дать описание всех групп из класса


        Первая задача имеет следующее решение.

        {Теорема}.
        Всякое связное локально односвязное\footnote{Локально односвязным
        в данном случае считается такое пространство

        у которого любая точка обладает по крайней мере одной односвязной
        окрестностью.} пространство

        обладает универсальным накрывающим пространством


        Даказательство этой теоремы громоздко и требует получения ряда
        промежуточных результатов, поэтому мы отсылаем читателя к книге


        Обратимся к более наглядному гомотопическому определению
        односвязности. Исходным объектом этой теории является {путь} ---
        непрерывное отображение

        отрезка

        в топологическое пространство (или многообразие)

        Последнее предполагается линейно связным, т. е. таким, что любые
        его две точки

        могут быть связаны непрерывным путем

        На множестве всех путей в

        можно ввести композицию, сопоставив двум путям

        результирующий. Разумеется, такое умножение определено лишь для
        тех пар путей, у которых начало второго пути совпадает с концом
        первого:


        {Замкнутым путем} называется путь, у которого конец совпадает с
        началом:

        Для совокупности

        всех замкнутых путей с общим началом, скажем, точкой

        введенное умножение определено для любой пары путей, и результат
        также является путем из этой совокупности. Тривиальный путь,
        состоящий из одной точки

        называется {единичным}. Отметим, что путь

        называемый обратным путем к

        отнюдь не является обратным элементом в

        относительно рассматриваемого умножения.

        Множество всех путей, и в частности

        разбивается на классы эквивалентности отношением гомотопии: два
        пути

        {гомотопны}, если существует непрерывное семейство путей

        такое, что

        (т. е. пути

        можно непрерывно деформировать или стягивать друг в друга).

        Композиция в

        естественным образом определяет умножение на множестве

        классов замкнутых гомотопных путей. Совокупность

        в отличие от

        оказывается группой!

        {Пример}.
        На рис.

        показан замкнутый путь на сфере

        (окружности), составленный из пути от точки

        до

        и обратного. Семейство путей

        и точки

        до

        (и обратно) стягивает этот путь в единичный. Путь на рис.

        очевидно принадлежит классу, отличному от единичного. Вместе с
        классами обратных к ним путей (и единичным классом на рис.

        ) их иножество образует группу, изоморфную


        Группа классов гомотопных путей, как оказывается, не зависит (с
        точностью до изоморфизма) от начальной точки

        и называется {фундаментальной группой топологического
        пространства (многообразия)}

        Она обозначается через


        В примере

        фундаментальной группой сферы

        является

        Однако при

        все сферы

        обладают тривиальной фундаментальной группой: любой замкнутый
        путь на них стягивается в точку (покажите).

        Групповое пространство

        мы представили шаром с отождествленными концами диаметров. Кроме
        единичного класса такое пространство допускает и еще один класс
        путей: его представителем может быть любой диаметр. Произведение
        таких путей очевидно принадлежит единичному классу --- группа

        (рис.
        ).

        Пространство

        называется односвязным, если его фундаментальная группа

        тривиальна. Эквивалентность этого определения данному ранее в

        следует из того, что любое накрытие односвязного (в
        гомотопическом смысле) пространства есть гомеоморфизм. Это
        доказывается в

        В этой же теореме устанавливается также универсальность
        пространства

        любое накрывающее пространство

        данного линейно связного, локально связного пространства

        в свою очередь накрывается его односвязным накрывающим


        Перейдем к задаче


        {Теорема}.
        Пусть линейно связное пространство

        является накрывающим пространством линейно связной, локально
        связной топологической группы

        Тогда на топологическом пространстве

        существует единичная с точностью до изоморфизма групповая
        структура, относительно которой накрывающее отображение

        гомоморфизм топологических групп, причем его ядро

        дискретно. (Доказать эту теорему можно гомотопическим методом

        .)

        Такая группа

        называется {накрывающей группой} топологической группы


        В решении задачи

        центральную роль играет односвязная накрывающая группа. Это видно
        из теоремы, которую мы назовем {принципом монодромии} (ее
        доказательство читатель найдет в
        ).

        {Принцип монодромии}.
        Пусть

        --- односвязная топологичкская группа и

        --- локальный гомоморфизм группы

        в связную группу

        Если при этом окрестность

        связна, то существует продолжение

        локального гомоморфизма

        до глобального, и оно единственно.

        Односвязная накрывающая группа

        топологической группы

        называется ее {универсальной накрывающей группой}. Следующее
        утверждение связывает всойства универсального накрывающего
        отображения

        с фундаметальной группой пространства


        {Утверждение}.
        Пусть

        --- универсальная накрывающая группа топологической группы

        --- (универсальный) накрывающий гомоморфизм. Тогда фундаментальная
        группа пространства

        изоморфна ядру гомоморфизма


        Универсальная накрывающая группа

        какой-либо группы Ли

        из класса

        в действительности должна быть универсальной накрывающей для любой
        его группы. Это следует из того, что для любой пары локально
        изоморфных групп Ли

        группа

        из теоремы

        является накрывающей как для

        так и для

        Тогда она в в свою %%% ошибка
        очередь может быть накрыта универсальной накрывающей, скажем,
        группы

        В силу единственности универсальной накрывающей

        должен существовать изоморфизм

        равно как и для любой группы

        Итак, мы установили, что каждый класс локально изоморфных групп

        однозначно (с точностью до изоморфизма) определяет группу

        --- общую универсальную накрывающую для содержащихся в нем групп.
        Вместе с тем эта группа вполне характеризует содержимое данного
        класса


        {Теорема}.
        Пусть

        --- односвязная топологическая группа, и связная группа

        локально изоморфна ей (т. е.

        ). Тогда

        изоморфна факторгруппе

        по дискретной нормальной подгруппе

        принадлежащей ее центру:


        Несложное доказательство, основанное на принципе монодромии,
        предлагается в качестве самостоятельного упражнения.

        Итак, класс связных локально изоморфных групп Ли

        полностью определяется свойствами универсальной накрывающей.

        В гл.

        будет показано, что группа квантовомеханической симметрии
        односвязна. Таким свойством не обладает группа вращений

        ее фундаметальная группа

        Группа

        является односвязной (в качестве одной из реализаций ее группового
        пространства мы рассматривали сферу

        ) и накрывает группу

        Именно она предсказывает наблюдаемые значения собственных угловых
        моментов (спинов) элементарных частиц.

        Группа

        имеет строение, аналогичное строению

        однопараметрические подгруппы

        где

        --- угол поворота

        `` склеиваются '' в диаметрально противоположных точках

        Ее фундаментальная группа также изоморфна

        а универсальной накрывающей является группа

        Этот вопрос строго рассматривается в

        Отметим, что класс

        содержит (с точностью до изоморфизма) две (при

        нечетном) либо четыре группы (при четном

        ), поскольку тогда центр

        есть

        при

        --- при

        (см. там же).

        Односвязность групп

        выявляется с помощью следующего утверждения.

        {Утверждение}.
        Пусть

        --- связная группа Ли и

        --- ее связная подгруппа Ли. Если однородное пространство

        гомеоморфно сфере

        (при

        ), то


        Доказательство целиком основано на гомотопическом методе


        Группа

        сама гомеоморфна сфере

        поэтому

        Отметим, что

        Это пространство гомеоморфно сфере

        поэтому односвязность всех групп

        является следствием утверждения


        Поскольку группа

        гомеоморфна окружности в

        Следовательно, все группы

        неодносвязны: их фундаментальные группы


        Одним из полезных критериев односвязности является следующее
        утверждение.

        {Утверждение}.
        Прямое произведение

        топологических пространств

        односвязно тогда и только тогда, когда

        односвязны


        Например, представим общую линейную группу в виде

        Поскольку

        гомеоморфна

        с выколотой точкой и очевидно неодносвязно

        то группа

        также не является односвязной.

        Возьмем группу

        Отметим, что она тривиально односвязна при

        и рассмотрим ее орбиту

        в пространстве

        содержащую точку

        можно привести в любую точку

        за исключением начала

        Минимальное нетривиальное значение

        для данного случая равно 2, а пространство

        очевидно односвязно. Таким образом, в расположении пространства

        необходимо рассмотреть

        --- группу стабильности точки

        Она представляет собой множество матриц вида

        где очевидно

        Заодно отметим, что из закона композиции преобразований

        вида

        следует, что

        есть полупрямое произведение группы

        и группы трансляций

        в

        Последняя изоморфна аддитивной группе

        и поэтому односвязна. В итоге приходим к выводу, что односвязност
        группы

        определяется односвязностью

        и, следовательно, все группы

        односвязны.

        Подчеркнем, что первая нетривиальная группа в рассмотренной серии

        точнее, ее вещественная реализация, обозначаемая

        --- одна из важнейших в квантовой теории элементарных частиц,
        поскольку она является универсальной накрывающей для связной
        компоненты группы Лоренца --- собственной ортохронной группы

        Построим гомоморфизм

        осуществляющий универсальное накрытие.

        Обратимся к

        -линейному подпространству

        эрмитовых матриц


        Разложим их по базису, составленному из матриц Паули


        С помощью инволюции

        зададим

        -билинейную форму

        положив

        Матрица этой формы совпадает с метрическим тензором в пространстве
        Минковского

        что позволяет отождествить его с


        Рассмотрим

        -линейные преобразования в

        переводящее

        в себя:

        где матрица

        Покажем, что при такой реализации действия группы

        на

        форма

        инвариантна. Напомним, что линейное преобразование тогда и только
        тогда сохраняет симметричную форму

        когда оно сохраняет определяемую ею норму

        для всех векторов линейного пространства. Отметим, что

        совпадает с определителем матрицы

        следовательно, соотношение

        определяет гомоморфизм

        группы

        в группу Лоренца


        Обозначим через

        элементы базиса в

        дуального к

        относительно формы

        Тогда


        Явное выражение матричных элементов

        через матричные элементы матрицы

        миеет вид

        Из аналитичности этих функций следует, что

        отображает связную группу

        в компоненту единицы группы Лоренца ---

        Покажем, что

        --- отображение `` на ''. Подгуппа

        посредством

        отображается на всю подгруппу

        Поэтому достаточно удостоверится в транзитивности действия

        группы

        на орбите

        содержащей точку

        Поскольку уравнение

        для любой эрмитовой матрицы

        (при

        ), как известно, всегда имеет решение

        (более того, среди всех таких

        существует по крайней мере одна эрмитова матрица

        ), то искомая транзитивность тем самым установлена. Ядром
        отображения

        очевидно является центр группы

        Итак, мы убедились, что группа

        является универсальной накрывающей для ортохронной группы Лоренца:













































